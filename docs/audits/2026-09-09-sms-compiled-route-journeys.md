# Offline compiled SMS route journeys

Status: **All 55 repaired-source compiled journeys PASS, with all six completed parents independently reviewed.** The run contains five normal journeys, 30 actual process-kill recoveries and 20 queued-message bursts across electrical, plumbing, roofing, painting and solar. All six wrapper and journey parents exited 0; every recorded child had closed at its parent exit. The earlier 25-case checkpoint and real roofing failure remain historical and contribute no cases to this result.

The [completed matrix receipt](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/compiled-route-final-55-02/verified-completed-matrix.json), SHA256 `f5b3ca824f3d758a5fde012c0fef9fd6a778b3f79d1dcd2c307ffc93e7ef8205`, binds all six exit/result/log/before/after receipts, six independent parent reviews and five recovery-detail reviews. Its final verification matched **4,636 named current inputs and artifacts**, including the unchanged harness and attested compiled inventories. Across these isolated cases, **408 accepted outbox intents matched 408 fake-carrier calls**, with 55 held results, 75 review tasks and 55 accepted/replayed held-quote follow-ups. These counts include the additional task for each of the 20 material-correction bursts. The 1,027 closed child invocations are summed within their completed parents; operating-system PIDs are not assumed unique across the whole run.

The [independent completed-matrix review](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/compiled-route-final-55-02/independent-completed-matrix-review.json), SHA256 `3c3d34a7a81654211597f720e13c7c238aea41d8dfb53d8aa465a0b95600b478`, rechecked those artifacts, identities, counts and all 4,636 current named hashes without starting another journey.

The [pinned six-command plan](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/compiled-route-final-55-02/execution-plan.json) ran roofing recovery first, then all five normal cases, followed by electrical, plumbing, painting and solar recovery in separate serial parents. Each case creates its own in-memory database and provider state, so no prior batch result is required. Each trade covers kills **before-history, after-history, before-result-save, after-result-save, after-publication and before-unlock**, plus bursts **before-history, after-history, before-result-save and before-unlock**. It does not claim every durable transition or deployed concurrency. The plan preserves its preparation fields; the completed exit receipts establish execution status.

| Completed parent, in execution order | Cases | Recorded children closed | Parent duration, seconds |
|---|---:|---:|---:|
| Roofing recovery | 10 | 198 | 586.490 |
| Normal journeys, all five trades | 5 | 67 | 310.521 |
| Electrical recovery | 10 | 168 | 496.458 |
| Plumbing recovery | 10 | 168 | 470.016 |
| Painting recovery | 10 | 198 | 516.219 |
| Solar recovery | 10 | 228 | 575.088 |

Execution ran from `2026-09-09T11:30:36.526Z` to `2026-09-09T12:23:52.415Z`: 53 minutes 15.889 seconds including serial handoffs, with 49 minutes 14.792 seconds inside the six parents. The actual duration supersedes the preparation estimate. No application, candidate or harness source changed during this run.

Readiness `f95126d0af7467d03dbc9fae433f7bf81ab6b2c31f89a601567a0c3dfde1da0c` was verified and pinned before execution. It binds five fresh trade builds and the independently verified unchanged front desk; all six service identities passed the fleet gate. The separate typecheck07 and production build08 passed after the browser fixture's 16 controls passed. Full06 itself remains **10,273 passed, one unrelated browser assertion failed, 24 skipped, parent exit 1 and no build**; these later gates do not turn that historical suite into an all-green run. The [source bridge](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/final-chain-full-platform-06/subsequent-tsc07-build08/verified-bridge.json) records the distinction. An initial evidence-preflight attempt refused the front desk because it was incorrectly subjected to the five-trade website contract; no journey child had started. The preserved corrected preflight verifies its actual source/build contract and retains all five trade contract checks.

## Preserved 25-case checkpoint and roofing failure

The immutable `compiled-route-final-55-01` directory contains three complete passing parents, each independently reviewed: all-normal five (67 closed recorded children), electrical recovery ten (168), and plumbing recovery ten (168). Both recovery parents cover the exact six kill and four burst boundaries above, with unchanged consumed inputs and prices, one chosen correction task/notification and exact persisted-intent/transcript assertions. Their result SHA256 values are respectively `3b7af0adf586adf9677d11dd430eea4910821431738d3e1eb432daf14c9e7270`, `a5b321278a22e2e13f8a85774e6407e6b7f5a257620a5d821591695bbf583676` and `d8fefb1d042c6f3b92ef564f80cb0f43d5c4a73afe7a36a36940ac9e28e7604e`. Parent and child exit receipts are 0; source, compiled inventory, manifest, attestation, readiness and harness checks passed before and after each complete parent.

Roofing's next parent exited 1 at the first burst (`before-history`), after six individual kill PASS lines. The actual first-name correction had already sent a second intake-form opener and replaced the held roofing state with empty slots and a null pending quote token. The unchanged assertion at harness line 282 correctly failed because `workflow_stage` was no longer `awaiting_review`. The saved measurement and generic quote reference survived, but those surviving fields did not make the extra message and state reset correct. All 105 recorded children from that failed parent's completed cases and current burst were closed. Its failure snapshot and logs remain; it has no complete roofing result. Painting and solar recovery were not started.

The [roofing repair evidence](2026-09-09-sms-roofing-saved-followup-evidence.md) records the reproduced failure, checked name persistence and ordinary follow-up protection, preserved new-job/cross-trade behavior, and **275/275 focused tests** with lint and independent review passing. This changed the canonical inbound source pinned by every trade candidate. The new 55-case matrix above therefore uses the freshly built candidates and the unchanged harness; the previously failing roofing burst now passes its original saved-state assertion.

## Preserved 15-case checkpoint

Five normal compiled journeys and all ten electrical recovery cases passed at this earlier checkpoint. The other four complete recovery parents had not passed. These historical results bind readiness `1ad6ddcf763aad220364e678f3efe65cc4698bd6eb12cc302ab970af65d16576` from `final-corrections-refresh`, the complete attested candidate inventories and runner `f3a2aedaf072af802bb1af1d6e4a04ba5bff733a32f434dd2148098f2373a023`. They precede the F18 fallback and later website repairs. All 55 cases must run against the refreshed freeze.

The exact four scripts are archived in `route-journey-harness-claim-stage-final/`. `compiled-route-final-corrections-checkpoint.json` binds the two completed result files, numbered stdout, exits, harness/candidate hashes and per-case counts. It explicitly records `fullMatrixComplete=false`; the filenames' word "final" does not certify an unfinished matrix.

| Completed checkpoint | Cases | Child processes | Unique SIDs, all replayed | Completed jobs | Held results | Review tasks / owner notifications | Accepted customer publications | Total accepted intents / carrier calls |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Normal: all five trades | 5 | 67 | 19 | 23 | 5 | 5 | 23 | 28 / 28 |
| Electrical: six kills, four bursts | 10 | 168 | 46 | 68 | 10 | 14 | 66 | 80 / 80 |

The normal result `compiled-route-final-corrections-normal-attempt-02.json` has SHA256 `b8c1647b34679b437eecc18e6eb820bb0c224ef467360f2d305a42f0cf0252f2`; its matching `.log` has SHA256 `8e1df43198f90d567bd6934ba8362dbe0aeb71ad49246ebe7b60bf49c32dcf3d`. Electrical's `compiled-route-final-corrections-electrical-recovery-attempt-02.json` has SHA256 `2977828a8097e01526fc0b64a0b78794cfd3fe6139dd492a35a10c7acf8177a3`; its `.log` has SHA256 `27d303e4bca3365e108ff7cc4b8c28e7da642daac127697051c12eedf7827bae`. Both have independently captured exit 0 records. The electrical bursts preserve exactly four address-correction tasks/acknowledgements/owner alerts with unchanged original inputs and prices. The two expected Alex-SID intake wakeups are separately validated as completed, idempotent recovery with no extra pricing or send.

After electrical completed, the parent requested a hold for the F18 correction. The serial wrapper had already admitted plumbing before a stop marker reached its next-run guard. The verified owned process tree was terminated during plumbing's third case, after two case PASS lines. Its partial stdout is preserved in `compiled-route-final-corrections-plumbing-recovery-attempt-02.log`, with an explicit `-interrupted.json` record; there is no completed plumbing result and those two lines are not accepted matrix evidence. All owned processes were confirmed closed before releasing the execution slot.

The first current recovery attempt retained six passing electrical kill cases before an added assertion failed at the fourth input's raw claim. The actual signed receipt is enqueued before tenant lookup, so its claim correctly has `tenant_id=null`; the handler attributes the exact owner before choosing or completing correction work. The harness had incorrectly required that attribution at claim. The correction requires exactly null at the raw claim, binds its complete signed envelope, work key, actual serial key and owner to the returned claim, and still requires the exact tenant at the chosen/completed boundaries and on the task. All unchanged-price/input/model/pricer assertions remain. That failure and captured state remain in `compiled-route-final-corrections-electrical-recovery-attempt-01.*`; the focused corrected burst passed, independent review and scoped lint passed, and all final 55 cases must use the corrected hash. This was a harness assertion repair, not a production code change.

## Historical checkpoints and development findings

The earlier results below remain historical evidence; their case counts are not added to the current checkpoint or a future final matrix. Statements about incomplete work in this section describe the checkpoint at that time.

Attempt 13 completed with exit 0 at `2026-09-08T21:27:15.980Z`, bound to the refreshed readiness created at `2026-09-08T21:24:12.0345853Z`. Electrical, plumbing, roofing, painting and solar each produced one priced held result and one notified review task, survived duplicate receipts in fresh processes, and answered a new held-quote link request without releasing a price or repricing. Painting used the natural word "repainting". All messages passed the strict configured website-origin assertion while `APP_URL` remained a different engine origin. No fixture or production source changes were made during this successful matrix.

The matrix used 67 child processes. It completed 23 durable jobs from 19 unique inbound SIDs and replayed all 19 SIDs without extra work. All 28 fake-carrier calls matched accepted persisted intents: five owner notifications and 23 customer messages, each customer message with exactly one attributed transcript publication. These totals include the five new held-quote follow-ups.

| Trade | Initial enquiry SIDs | Completed jobs, including follow-up | Saved family result | Notified review tasks | Accepted intents / fake-carrier calls | Child processes |
|---|---:|---:|---:|---:|---:|---:|
| Electrical | 2 | 5 | 1 | 1 | 6 / 6 | 11 |
| Plumbing | 2 | 5 | 1 | 1 | 6 / 6 | 11 |
| Roofing | 3 | 4 | 1 | 1 | 5 / 5 | 14 |
| Painting | 3 | 4 | 1 | 1 | 5 / 5 | 14 |
| Solar | 4 | 5 | 1 | 1 | 6 / 6 | 17 |

Evidence is retained under `C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/`:

- `compiled-route-attempt-13.log`: complete numbered final stdout and all five passing journeys; the process exit was 0.
- `compiled-route-journeys.json`: per-trade results, child process IDs, real function/provider observations, saved resource/task/job identities, held follow-up assertions, complete executed candidate hashes and four harness hashes.
- `compiled-route-final-validation.json`: observed process exit, source tool evidence IDs, result/stdout hashes and compact per-trade totals.
- `final-ready.json`: the bound readiness SHA256 was `3f70e3787b5834845294b9153988139d73f86c869f2982a669623cb3208f8303`; its candidate evidence directory is `schema-readiness-refresh`.
- `compiled-route-final-lint.log`: post-matrix scoped ESLint exit 0 with no output, warnings or errors. All four files also passed `node --check` before the matrix and their hashes stayed unchanged throughout it.

Earlier numbered failures and debug results remain historical evidence. They exposed the painting recogniser and direct-handler origin defects and prompted the specialist state-recovery review. Temporarily supported painting wording was used only to diagnose later stages; it was restored before the final successful run.

The four harness files used for this passing checkpoint are preserved byte-for-byte in `route-journey-harness-v1/` beside the evidence, with the hashes recorded in `compiled-route-journeys.json`. A subsequent recovery mode exercises actual electrical and painting handler process loss and queued corrections/price questions/resend requests. Its electrical before-history kill pilot passed (attempt 14, with matching files archived in `route-journey-harness-recovery-pilot/`). Attempts 15–16 stopped at an overly narrow exact job-count assertion: captured state proved that a correction arriving before the first estimate saves its quote can enqueue one idempotent intake wakeup. It returns the original owned intake/held result without additional model/pricing work, quote, task or message. The updated test retains the exact original job chain and separately validates only this correction-SID wakeup in the two observed electrical timings.

At the attempts 17–19 checkpoint, recovery acceptance remained pending after a confirmed duplicate-photo defect. Those attempts exercised all 20 distinct scenarios with their then-current assertions: 12 actual handler kills and eight queued-message timings across electrical and painting. Attempt 17 contains nine completed cases before a second wakeup-count failure, attempt 18 contains six before an unregistered painting model-fallback fixture, and attempt 19 completes the remaining five. Their exact harness versions are archived separately. The compact [development record](2026-09-09-sms-compiled-route-recovery-development.json) preserves those source bindings and whole-attempt failures; it is not a single successful frozen run.

Consolidation found seven accepted intents in electrical `kill:before-unlock`, where six were expected including its later held-status question. A focused actual POST/outbox regression reproduced two different random photo templates accepted for the same customer/upload token after interruption. The old harness asserted one publication per intent but lacked an exact initial intent count, so its case-level PASS was insufficient. `photo-replay-before-fix.log` preserves the failing bodies. The canonical route now checkpoints the first photo body and uses one job-specific photo intent key; checkpoint failure and a resolved enqueue failure without an intent ID both retry the original work. `photo-replay-final-tests.log` records 32/32 passing actual-route cases, including those three photo regressions; scoped lint and independent source review passed. The compiled harness now enforces exact initial intent counts in every normal/crash/burst case and forces different random variants in the electrical pre-unlock kill. It also requires the painting form opener to explain painter review. At that checkpoint, rebuilt candidates and a complete 20-case recovery run plus all five normal journeys were still required. New harness hashes must not be substituted for the archived checkpoints.

A subsequent review of the photo flag's locally ignored PATCH result did **not** establish another runtime defect. `withFencedSmsClient` records resolved mutation errors and the actual worker rejects completion after draining callbacks. `photo-flag-worker-tests.log` records two passing probes using the real Supabase PATCH builder, fence and worker with an offline fetch boundary: returned database errors and transport errors both request retry before completion; a repaired second attempt saves the flag. No photo flag runtime change was made.

The subsequent harness version admitted all five trades for six real handler kill boundaries and four queued-message timings (50 cases), while retaining all five normal journeys. Its prior 20-case source is preserved in `route-journey-harness-photo-frozen-20/`. Solar result-save pauses now target its actual atomic `sms_save_solar_estimate` RPC, validate the actual worker owner and require a returned saved ID/token; they do not invent an intermediate table-write stage. The expanded matrix had not run at that source checkpoint; the later execution status is recorded above.

That expansion exposed a real solar follow-up gap: a saved solar reference caused every non-quote-action message to return held-status boilerplate before slot extraction. `solar-name-before-fix.log` records the actual POST regression failing with zero extractor calls for the Alex correction. The narrow route change checkpoints the existing extractor, persists only the customer's name while retaining solar/reference/review data, then acknowledges the change without entering a draft pipeline. `solar-name-final-tests.log` records 50 passing focused cases across the actual POST fixture, solar helpers and the photo worker probe. New solar coverage includes five interrupted persistence/checkpoint/unlock cases, changed model output on replay, exact one accepted reply/transcript, held price/link server-action controls and a fresh deterministic solar intake control. Database/model/provider boundaries remain fixtures in these focused POST tests; the actual compiled five-trade recovery matrix remained an execution gate at that checkpoint.

The original three-input queued correction was specifically a first-name change to Alex persisted in owned conversation slots. These cases do not prove address/scope correction or repricing. The recovery runner uses the actual route-assigned serial keys: a competing inbound worker remains blocked by a paused inbound job, but may process the customer's FIFO inputs while an estimate owns a different key. Only a confirmed killed child's abandoned lease deadline is advanced for the restart assertion. PGlite still serialises the fixture database accesses; this is not production multi-connection timing certification.

The following source revision added a fourth burst input: `Correction: the installation address is 22 New Road, Sydney NSW 2000.` It retains the existing name, price-question and resend inputs, duplicate signed receipts, actual route serial keys, exact photo counts and narrowly identified generic intake wakeup. Each burst must now produce exactly four customer replies, one additional owner notification and one correction task keyed to the fourth SID. The task must contain the original correction text, belong to the owned conversation and point to the same saved family result. A resource ID may be absent only when the generic result does not yet exist and an owned intake is demonstrably queued or running. Normal and kill scenario counts remain unchanged. This extension had not run at that source checkpoint; its four electrical timings later passed as recorded above.

For the material correction, the parent records the actual `claim_sms_work` response before the business handler receives it, then its `saved_job_correction` checkpoint and successful `finish_sms_work`. It requires unchanged priced rows and consumed inputs, no additional intake job, no model or pricer calls, exactly one owner alert plus one customer acknowledgement, and unchanged tasks/intents on fresh-process duplicate replay. The original confirmed address stays on the saved result; the requested replacement is retained for tradie review and is not automatically applied or repriced. The four scripts immediately before this addition are preserved in `route-journey-harness-pre-address-correction/` with checked hashes. New execution must bind the new harness hashes and freshly attested candidate inventories rather than reuse older proof.

Active solar gathering also needed a separate correction fix before that final compilation. The earlier helper lost cross-step phase/panel answers and retained stale address confirmation; requested system size was never captured. The bounded helper now captures explicit phase, panel and finite positive kW inputs, asks for clarification on conflicting or negated choices, shares the canonical size maximum and clears address confirmation after a changed street/postcode/state. Invalid saved size or unresolved clarification cannot reach pricing, while saved-result replay still returns the original result first. `solar-gather-corrections-before-fix.log`, `solar-gather-size-negation-before-fix.log` and the range-parser attempt preserve failures; `solar-gather-corrections-final.log` records 56/56 focused helper and actual estimator-argument controls. Scoped lint and independent source review passed. These are scripted provider/DB boundary tests, not completed compiled journeys or live model/carrier proof.

## Harness contract and evidence limits

The runner is `quotemate-automation/scripts/test-sms-route-journeys.mjs`. Its three companion scripts provide a parent-owned PGlite database, explicit provider fixtures and a child-process loader. Each child imports the candidate's actual compiled POST handlers directly, bypassing generated `main.ts` bootstrap and HTTP transport. Electrical and plumbing pass through inbound, intake and estimate; roofing, painting and solar execute their actual specialists inside inbound, matching the production dispatch path. Delegating function observers call the original compiled structurer, estimator and trade pricing functions and record their invocation.

The test database applies actual migrations 122, 154, 190, 191, 198, 199, 201 and 207 over an explicitly limited fixture schema. Work leases/checkpoints, publication/outbox RPCs, idempotency constraints, tenant review tasks, form offers and generic immutable pricing versions therefore run in PostgreSQL through PGlite. Push-event SQL executes with no registered devices. A test-only PostgREST-shaped adapter supplies the route query-builder boundary. It is not evidence of complete production schema, RLS or PostgREST compatibility.

Model decisions and Google responses are scripted fixtures. Roof and painting use the existing deterministic mock measurement providers when their live provider keys are absent. The actual orchestration, pricing, persistence, approval hold and durable publication code executes unchanged. Optional RAG, tenant filestore, generated imagery and quote PDF MMS are disabled through existing configuration. No live credentials, remote database, model, carrier, approval, payment or PDF operation is used.

Before creating the test database, the runner binds the candidate to `final-ready.json`: metadata paths must match the candidate directory, manifest and build-attestation hashes must match readiness, and the attested source hash must match readiness. The complete recursive `dist` inventory and every file's bytes must match the build attestation. The entire compiled inventory and metadata are checked again after the journey; readiness and harness files must remain unchanged throughout the matrix.

Successful journeys must prove:

- Durable receipt exists before the receipt child exits.
- Actual pricing functions execute and one saved result contains finite positive priced tiers, with an explicit quotable decision and no customer release.
- Generic quotes retain their actual immutable pricing version and are awaiting tradie approval.
- Exactly one original durable review task links to the saved result and records its configured owner's successful notification; material-correction bursts add exactly the separate SID-keyed task and notification described above.
- Every expected fake-carrier call has one accepted outbox intent with its matching SID; each customer intent publishes exactly one matching conversation transcript row.
- Every generated public link in the fixture messages uses the configured public website origin, including photo-upload links.
- All route work completes. New child processes receiving the original SIDs again perform no business work, model calls, additional sends or writes to the saved result.
- A new signed receipt asking for the held quote link resolves the same saved result and produces exactly one accepted review-status reply with one attributed transcript row, without exposing a price/link or rerunning a model/pricer. Replaying this new SID in another process performs no work or send.
- Any unsupported adapter, model or external transport call fails the successful-journey test even if production catches the error.

The current 55-case matrix covers normal completion across all five trades, the six named actual-route kill boundaries and four queued burst timings for each, duplicate replay after process restarts and a new-message follow-up on a held quote. It does not certify an owner-approved release/resend, process loss at every point within the actual route, live model quality, real carrier delivery, concurrent production Postgres sessions or deployed tenant behaviour. The separate compiled primitive process-kill harness supplies narrower evidence at its stated kill and pause boundaries; its fixture stages must not be relabelled as actual route journeys. Origin findings apply to direct handler/website conflicting configuration: generated service startup already validates and normalises these origin variables before importing the application, so the captures do not prove a wrong host through normal generated startup.
