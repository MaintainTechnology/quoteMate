# Actual additional-tool creation through owner release

**PASS: both composed cases against actual221**, executed in [five-group checkpoint05](2026-09-09-sms-standalone-ci-evidence.md), with confirmed child exit0, no skipped/failed cases, unexpected operations or blocked network calls. The current [composition JSON](2026-09-09-sms-created-tool-release.json) SHA256 is `f2f4daf0954f3be985b32c36b08db01a302a091dbcf2cd2ee06c4f5d34da9c28`; it binds 46 named source boundaries. The complete CI archive binds 334 inputs. Four changed fixture/test files passed scoped zero-warning lint.

The commercial case now starts with raw unpriced takeoff data, reads the actual corrections GET baseline and submits that exact revision/items to the actual POST and221 transaction. Invalid quantity plus metadata returns400 and leaves both rows unchanged. Exact operation replay/readback retains one receipt; changed-payload reuse and stale correction/repricing return409 without state changes. Actual219 pricing then consumes the confirmed revision; quiet Save retains its exact pricingProof/pricedAt pair and produces the independently asserted **A$2,481.95** generic draft. No confirmed result, BOM, proof or receipt is fabricated.

The same rows continue through generic owner approval/public rendering/resend, followed by separate rich-run review and approval using221's exact source/proof/generation binding. Commercial calculations are explicitly **two producer calls** (Price and Save verification), **one review GET validation** and **one approval POST validation**, four overall. Changing the physical labour rate from A$95 to A$190 after release returns409 before another calculation, preserving the approved BOM, identity and A$2,482 rounded rich display. Late replay of the original confirmation returns its earlier receipt without changing the released result. The aircon case remains one actual creation calculation plus release/reopen/resend/replay. Together the cases retain five accepted fixture-carrier calls.

The helper executes actual107 rate-table definitions, actual219 and actual221 SQL over the shared owner fixture; helper SHA256 `445ddc1a5aacc20e1d1939e7da867c77a0dc5ee7e8d5b02cb592f0534086ba51`, test `54449a451b77f917324ac60e7f60a9b9fffadbf950ec01ff5cd724673b844417`. The [independent final-handoff review](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/221-composition-handoff-independent-review.json) verifies adapter signatures and all17 canonical source hashes. Actual browser editing, provider/model quality, production PostgREST/RLS, multiconnection timing, PDF rendering and deployed delivery remain outside this composition. Checkpoint04 also passed both cases but failed six separate owner-PDF cache seeds; its overall115/6 result is preserved before the test-only repair and complete05 rerun.

## Preserved migration 219 composition

**PASS: both offline composed cases**, executed in the earlier exact five-group checkpoint03. The child exited0 with two completed cases, no failures, skipped cases, unexpected operations or external network attempts. That composition retained **five accepted fixture-carrier calls**, one aircon pricer call and two commercial pricer calls. Its [archived JSON](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/standalone-ci-five-group-final-03/website-composed-tests/created-tool-release/composition/2026-09-09-sms-created-tool-release.json) SHA256 is `5b320d2f05aaabb951aab7aad18e14d76e22bb6aebc9231159b3c51521ff6d3f`; it records 40 named source hashes. The statements in this section describe that pre221 checkpoint.

Commercial pricing now executes actual migration 219's source snapshot, proof persistence and atomic-save functions over the physical rate table/index from migration 107. Inputs are explicit owned tenant rates and an unpriced commercial takeoff; no default prices, generated BOM or proof is substituted. The Save request forwards the exact `pricingProof` and six-digit UTC `pricedAt` returned by the actual Price handler. The inherited labour basis is verified as tenant-controlled. The same reviewed pair is retained on replay.

The actual save remains quiet, creates one generic owned draft and retains the original **A$2,481.95** price. Owner approval exposes that generic document, and its saved-reference resend reuses one intended delivery. The separate rich-run public document stays held until its own explicit approval. After that approval, changing the physical tenant labour rate from A$95 to A$190 and invoking the real price route returns 409 `released_quote_immutable`, with no third pricer call or change to the approved BOM, token or public amount. The rich document's existing rounded display is A$2,482; the generic document shows the exact saved A$2,481.95.

The shared fixture retains actual migrations 215 and 217, the baseline 30% deposit default and 211/212 release protections. The unique commercial helper SHA256 is `01677dd6a8d145a58a3062a0d9a0733fc6e2936bc5b8c843869884937445374c`; the created fixture is `f34908bec184117e7eef7c3641e1afccf03e073cf714064952819e51ee9d711d`; the test is `24ce0f5c313823a87eb1fbc9fcf80a9154f0201cba80dcfa7a91c2e449e010be`. Actual migration 219 is `17f5ccffc6104b09baf78ee5403a4916c28ee1443def1d21143c63d6da005de0`. Before/after source checks and the archived source inventory passed; scoped fixture/test ESLint and bounded independent preparation reviews passed.

Checkpoint02 stopped before this group because the first group's consumers omitted `pricedAt`. Both prepared consumers were updated to use the actual returned reviewed pair, then checkpoint03 ran all groups. No runtime pricing/release change was made to pass this refresh. The prior BE01 checkpoint and its JSON remain archived below and in checkpoint03's `prior-compositions` directory.

This seeds a run with `status: 'ready'`, raw extraction `items` and `corrected_items: null`; it does not execute the Confirm PATCH or establish a confirmation receipt. The old fixture description calling that input "already-confirmed" was broader than the executed evidence. Owner auth, location evidence and carrier acceptance are fixtures. The database is a minimal local PGlite/PostgREST-shaped adapter; no production RLS, real provider delivery, PDF rendering, native UI, payment or deployed inbound SMS journey is certified. The fuller shared source archive binds additional static imports, while this composition's own 40 hashes remain named boundaries.

## Historical BE01 and earlier integration evidence

Historical checkpoint: **both offline composed cases passed** in `be01-created-checkpoint-03/created` with observed exit 0. That JSON had `completed: true`, two passing results, empty failure/unexpected-I/O arrays and five accepted fixture-carrier calls; SHA256 `443074ed9e66aeb3fb56e4976380ea48cc2e7025c251282ac168e4e017540cd6`. It preceded the migration 219 and exact five-group refresh above.

The fixture now applies the actual baseline `deposit_pct DEFAULT 30` before commercial creation, without changing a generated row afterward. The generic page prints the exact saved $2,481.95 while the separate rich document retains its rounded $2,482 display. These are distinct display assertions over the same unchanged persisted amount. Checkpoint 02 passed the owner suite but failed this old rounded generic assertion; checkpoint 03 passes both created-tool cases after correcting only that expectation. See [fixture checkpoint evidence](2026-09-09-sms-be01-fixture-checkpoint.md).

The latest integration refresh retains migrations 211 and 212 and the correction-aware origin resolver in the common owner-release fixture and named hashes. Both cases pass with that guard baseline and resolver through the offline CI runner. Attempt 06 is the earlier passing 211-only checkpoint; attempt 07 is the passing 211/212 checkpoint before correction integration, also preserved as `created-tool-release-before-correction.json`. Attempt 08 remains archived before this CI refresh.

The dedicated runner `quotemate-automation/tests/sms-created-tool-release.test.mjs` calls actual aircon recommendation, commercial pricing/save, owner approval, dashboard saved-job and public page handlers over the same local PGlite rows. It uses the existing owner-release SQL adapter plus a new narrow persisted tenant-rate relation. Real deterministic pricing exports are wrapped only to count calls; their return values are unmodified. No generated recommendation, BOM or quote is manually seeded. Commercial input is an unpriced raw takeoff on a ready run; extraction creation, Confirm PATCH and confirmation UI are outside this composition.

The passing aircon case created one priced owned recommendation, held its public prices until explicit owner approval, accepted one initial SMS, reopened the actual public page and dashboard link, and sent the saved reference into a new conversation. Replaying creation and approval/resend retained the saved identity and message intent. The actual pricer and external-location fixture were each called once. Customer contact was supplied through the existing owner-approval field because the creation request does not collect it.

The commercial case initially returned `tenant_pricing_required` during save despite unchanged tenant rates and takeoff. `priceOne` emits an own `height_m` property with value `undefined`; PostgreSQL JSONB drops it, whereas JavaScript recomputation retains it. The save route's raw `isDeepStrictEqual` comparison therefore rejected equivalent prices. The correction compares the stored JSON representation after the existing authority checks, and the exact original omitted-height input now saves successfully. The diagnostic records raw strict equality false and JSON equivalence true. Earlier in-memory fixtures used `structuredClone`, which retained undefined fields; they now round-trip through JSON. Attempts 02, 05 and 06 retain the actual before/after evidence.

The later part of the case confirms actual save → dashboard manual send → public generic quote → accepted saved-reference resend and both creation/send replays. The returned generic quote is released, while the separate dashboard rich-run link remains held. This is recorded as two distinct existing documents, without assuming that approving one must approve both.

After explicitly approving the rich run, the test changes only the persisted tenant labour rate from $95 to $190 and invokes the actual pricing handler again. Attempt 05 exposed a newly calculated **$4,780.95** through the same public token instead of the approved **$2,481.95**; the page displayed rounded totals. The run's `released_at` timestamp and generic saved quote remained unchanged. This reproduced the public page's old release stamp combined with its latest-extraction read.

The final case executes migration 211 and the corrected pricing route. The same repricing attempt now returns HTTP **409 `released_quote_immutable`** before another pricer call. The saved BOM, released timestamp and public token are unchanged, and actual public rendering retains the original $2,481.95 tender amount (displayed as $2,482). The route requires the actual trigger-readiness RPC before held pricing. Database triggers separately protect published extraction prices/selection/ownership and parent release identity; the independent 32-case SQL suite covers mutation, replacement, removal, RLS-hidden truncation, readiness drift and safe rollback. It does not demonstrate production multiconnection timing: a concurrent parent/extraction lock inversion can safely abort a transaction.

Across the two composed cases, aircon invokes its real pricer once and commercial invokes its real pricer twice (initial pricing and save verification). The five accepted fixture-carrier calls are aircon approval/resend, generic commercial approval/resend, and the separate rich-tender approval. The two new-conversation resends each have one attributed accepted transcript and retain their intent on replay. The three portal-origin approvals do not invent an SMS-origin conversation. All generated customer message URLs must use the configured public website origin.

The JSON artifact [2026-09-09-sms-created-tool-release.json](2026-09-09-sms-created-tool-release.json) records cases, responses, named source hashes and unexpected I/O; its current hash is above. The historical four-group CI report SHA256 is `7156048ac0d0f1f303e30664d8ddb6ddc75f7afc9d28638149d4f26f4b8fa2b5`, archived in `be01-fixture-checkpoint-01/previous-reports/`. Its combined log remains `standalone-ci-final-01/inline-ci.log`, SHA256 `272fa83e0ca0193403c6a6ea695ed2a76972e4d325daac0add418d5a36a49a51`. Each passing checkpoint requires both cases, fresh completion, empty failure/unexpected arrays and separate exit 0; named source files remain unchanged during execution. Migration 211 SHA256 is `7b0b2f0892fd1fe67489a1d8a731ae518591347e0fc0b88763421e6e3676c909`; migration 212 is `4046d1bb6687c99025ad5e2e42692c66dc58906cc90e6f875ebe1ba68d5887d9`. The direct focused command remains:

```text
pnpm exec vitest run --config scripts/vitest-sms-created-tool-release.config.mjs --maxWorkers=1
```

Logs live under `C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/`. Attempts 02, 05, 06, 07 and 08 also have archived JSON artifacts. Attempt 01 exposed an omitted inert database URL. Attempt 03 used the wrong existing action for a portal draft (`/approve` instead of the dashboard's `/send`), and attempt 04 exposed missing nullable quote-review columns in the minimal fixture schema. Those fixture/caller issues were corrected without changing production behavior or weakening the public-price assertion. Independent bounded harness/source review passed before the final run; the combined scoped ESLint is recorded in `owner-tool-plan-final-lint.log`, with the latest shared fixture edit covered by `correction-owner-harness-lint.log`.

The [exact standalone CI refresh](2026-09-09-sms-standalone-ci-evidence.md) retains the prior report under `standalone-ci-final-01/prior-reports/` and the fresh report plus runner/Vitest results under `website-composed-tests/created-tool-release/`. Its outer network log is empty and the child exit is confirmed. This is local execution of the workflow code, not a hosted CI run.

Owner authentication, external location evidence and carrier acceptance are fixtures. No live network, PDF renderer, payment, post-response archival or browser-only overlay executes. Public server components render static markup; PostgreSQL runs real approval/reference/outbox migrations over an explicitly minimal schema. This is not production RLS/PostgREST compatibility, provider delivery, a full SMS inbound journey or a deployed certification. Source hashes bind named boundaries, not every transitive import.
