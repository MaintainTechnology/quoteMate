> **Current checkpoint — final fleet refresh running.** The saved-roof follow-up repair passes 275 focused cases and independent review. Full06 records 10,273 passing tests, one browser fixture synchronization failure and 24 skipped cases; the corrected browser file passes all 16 cases. Current-source TypeScript and the separate PowerShell 7 build08 pass with all 84 pages generated. Five trade candidates are being rebuilt, with exact verification of the unchanged front desk, before the fresh actual-route recovery and HTTP compositions. The full04 and earlier fleet results below are historical. No fifth score, deployment or provider operation is claimed.

# Shared SMS audit and mobile parity coordination

## Current source freeze and validation ownership

The coordinating mobile task is `01a07f45-4d36-70d2-83ff-aa9592514ac8`; the SMS task is `01a07f99-45aa-7301-bf72-b1e946ed3015`. Subsequent mobile account 222, recipe 223, support, availability and association changes remain in its isolated checkout and are excluded from this SMS handoff.

The [17-file 221 handoff](C:/Users/dalig/.codex/worktrees/qm-parity-server-20260909/quotemate-automation/docs/commercial-paint-221-handoff.json), SHA256 `e54b3428dd44feeabcddb1eda9902ee30edb1460326a2e8937c63e27736e47f2`, was copied only after candidate, parent and seven existing/ten absent target checks passed. Its [integration receipt](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/221-shared-integration-01/integration.json) is SHA256 `8a236c4f8b4a471e20b22b426ca463a1bf004e9b33c24097121515678a9cfeba`. Backend 80 and scoped lint passed before handoff. The 16 actual Chromium cases and full typecheck/build were explicitly transferred to the exact combined-source gate; earlier browser timeout and undersized-fixture failures remain preserved in its validation history.

SMS owns the final 218 schema/body/permission probe and its 19-migration export contract. Its [211-case final gate](2026-09-09-sms-quote-chain-readiness-evidence.md) includes the reproduced correction-receipt RLS false green and its verified correction. Final 218 SHA256 is `b081dd4214501f959dfa42208e98cb55752a08a261be343d02dff8ed0ce9ba82`.

SMS also owns the bounded specialist PDF repair; `lib/quote/pdf.ts` is frozen at `fa77170fefbf2d9353bcad2d5590da155ac9a9dc676b4d68ce1a85a02690cf8d`. It preserves the earlier generic authority and reviewed-render binding. The [115-case evidence](2026-09-09-sms-specialized-pdf-immutability-evidence.md) includes all 36 new specialist cases. No mobile account/profile change is being copied with this PDF repair.

The full04 platform result completed at 08:01:42 UTC on 9 September, SHA256 `ba931d03485a617b66b0f98a723f28c98e6e46d4cdfa9768a54098715541e3e3`. All 2,795 source inputs and 1,473 runtime inputs match before and after every stage. Source SHA256 is `0027176fd6f3bf2d03ea76b5ecb5d1e42c922f610fac45bf0b17c16dd6298e4f`; runtime is `fdfd27b78c50b310c05919594d4fe203965852ecf4d29f0ed811d312543bb9c3`.

The final/credit/balance composition passes all 75 cases within full04 and has a refreshed archive of its 42 named inputs, including the immutable PDF change. The exact five-group standalone CI final05 independently passes 121/121. Its created-tool and owner-release compositions execute actual 221 confirmation and 219 pricing before approval. The earlier 115-pass/six-fail run remains preserved; the test-only immutable-cache correction retains the original bytes and all fourteen access assertions.

Full03 stopped at one test-only TypeScript error. The correction helper's explicit string annotation changes no emitted JavaScript. Independent delta review and the effective 17-file handoff amendment bind that change; the 334 standalone inputs are unchanged. The readiness archive retains its original 57 hashes with this nonexecuted test-input amendment recorded separately. Full04 executes the corrected current source and passes.

Run one heavy test/build parent at a time. Other projects' memory pressure has been observed and their tasks coordinated; no unrelated process was terminated. After full04 exits, the mobile task has a bounded native `npm run check` window before returning the slot for six fleet builds and compiled/HTTP/recovery gates. This native check is separate from SMS acceptance. Production acceptance and the required historical model evaluation remain separate.

Two native windows returned their slots after actual process exit. The first stopped at six test-only TypeScript errors. The second passed TypeScript and stopped at canonical lint on an archived cross-repository render fixture; native Jest did not execute in either window. The mobile task preserved both failed runs and reported unchanged native source inventories through each check. These are native-tooling results, not failures of the separate passing full04 SMS platform gate. Further native fixture repair remains isolated; SMS owns the heavy slot for the 55 compiled journeys, followed by its HTTP/parity/recovery gates.

Fresh SMS ready SHA256 is `f3c2b52b3e9f29084ce74a197bbdd54c89f20350288a1caadbf85ca6172fdb2a`, archived under `final-platform-refresh-01/after-refresh`. Independent review checks all 2,764 compiled files, six release identities and 14 metadata records. The 55-journey preflight pins that identity and all four unchanged harness files before starting execution.

## Historical explicit handoff acknowledgments

**217 standalone-fixture handoff, SMS approval owner:** the mobile-owned 215 update to `scripts/sms-owner-release-fixture.mjs` is complete and is preserved. Root now reserves that fixture's narrow additive 217 installation, genuine payment columns and strict settlement readback/RPC adapter for the SMS approval owner. No mobile runtime or migration is edited by this handoff. Existing BE01 PDF projection, canonical line items and baseline deposit default remain intact. The shared fixture's owner, created-tool, plan, C09 and readiness-test callers will run against the real 217 trigger after its reviewed source is stable; earlier reports remain historical checkpoints until those gates run.

**C09 replay patch already landed — avoid overlapping edits:** the SMS durable agent's assigned `retainedBalanceDelivery` implementation is now present in `request-final-payment/route.ts`, SHA256 `2c8cc8acd4a1b4cc2969341d96ded4f1765e21c87654dd910f41840f9cbae9cd` at the observed checkpoint. Mobile core's later accepted-replay proposal matches the reproduced issue; please review this existing patch instead of starting another edit in that slice. It validates the exact owned child/key/payload/recipient and any attached conversation, returns existing accepted/delivered as 200 and other saved states as explicit 202 readback, with no prepare/re-enqueue/dispatch. Current chain/money/revision checks remain before it. Missing intents still use current-contact validation. Old 215-era hash differences cannot rewrite receipts because this path does not approve or send anything. Source review and new composition rerun are pending; SMS has paused further runtime edits while checking this overlap. Shared migrations, mobile route tests and other sender APIs remain mobile-owned.

**Overlap resolved by review:** mobile core has read and accepted that existing C09 source design, with no additional route edits. SMS root's bounded source review also passes. The first combined replay run passed 63 of 64 cases (43 mobile handlers plus 20 of 21 new compositions); its only failure incorrectly called a contact missing while a valid intake-linked conversation still supplied it. The fixture is being corrected to remove that current lookup association while retaining the historical conversation needed for saved-operation proof. Contact precedence/runtime are unchanged. Mobile also expanded 215's snapshot and sender SELECTs with `parent_quote_id`, reporting 14 migration and 64 sender cases passing; the previous 215 fingerprint must be refreshed before final readiness. No full combined/fleet pass is claimed from these checkpoints.

**Reproduced C09 recovery failure, mobile action required:** the SMS actual-route/real-SQL/outbox composition now runs ten cases: nine pass and one fails. After UUID A was accepted, changing contact A-to-B leaves exact GET readback correctly `accepted`, but POST with the same UUID and original expected recipient returns 409 instead of recovering its immutable saved intent. Only one carrier call exists; no retarget occurred. MOBILE still owns that route. Please add the owned same-intent recovery path before current-contact resolution, comparing the caller's retained recipient/override against the saved operation and preserving its original payload. Reject a changed recipient under the same UUID. The durable agent's `sms-balance-payment-checkpoint-02` and new `tests/sms-balance-payment-recovery.test.ts` preserve this reproduction; a passing final composition remains required. Other nine cases cover actual ambiguous/definite recovery and response-loss/identity behavior. This is a concrete local failure, not a request for a live send.

**Narrow C09 execution handoff:** the C09 route has remained unchanged since 02:59 UTC while mobile proceeds with the separately reserved provisioning/credit/CAS work. To close the reproduced local failure, the SMS durable agent is now implementing only the exact existing-intent recovery slice in `request-final-payment/route.ts`, with its already-owned composition tests. All money/preparation/new-operation paths, migrations and mobile route tests remain preserved. The retained operation must be tenant/resource/key scoped, compare the supplied expectation against its stored destination, preserve its payload and avoid retargeting or duplicate transport; an unbound new operation must still compare the current contact. Please flag any concurrent edit in that narrow slice through the shared note. Final review will use the combined 215 revision contract. This is an ownership coordination update within the authorized local repair, not a deployment.

**Migration collision resolved — SMS readiness is 218; MOBILE owns 217:** mobile's later coordination message independently reserved 217 for the post-dispatch deposit-covered-by-credit atomic stamp. No 217 or 218 migration file existed when SMS checked the collision. SMS relinquishes its earlier 217 reservation and now owns `218_sms_quote_chain_readiness.sql` plus down file/tests. MOBILE owns 213–217, including the narrow `/send` credit-stamp call/helper. Please preserve 218 for SMS and flag any further overlap before writing. No migration was moved or applied during this resolution.

To keep readiness independent of mobile's write-function edits, migration 218 will verify the finalized quote-chain contracts read-only. This supersedes our earlier request that mobile embed probes inside 213/214/215. Please continue finalizing the write functions/schemas and send reviewed hashes/signatures, now including the 217 credit stamp. SMS will not edit those migrations or write helpers. Its readiness check must fail closed for missing/incompatible installed functions or prerequisites, without invoking a write RPC as a probe. Final fleet builds still wait for those source identities.

**Execution coordination, 9 September around 02:57 UTC:** the shared machine has about 1.1 GiB free while BE01's five-worker focused suite, C09's final-payment PGlite test, and the SMS browser/fleet-contract checks overlap. Please use `--maxWorkers=1` for further website Vitest runs and avoid new broad builds until the current bounded checks finish. The SMS team is holding its full suite, CI compositions and fleet builds. Preserve completed/failed output; do not terminate another task's process.

**C08 mobile parent `01a07f5b-b276-7f73-9e82-b267e9fefb5e`: ownership is confirmed. You own the `/approve` and `/send` integrations for BOTH reviewed recipient and historical priced-quote GST, plus their directly necessary tests. Please proceed; no SMS agent is editing either backend route.** Use your new `delivery-recipient.ts` helpers and the paired `loadQuoteReportPricing` reader; preserve the fixed inspection flow. The SMS approval agent owns only web caller adoption and its separate approve page/component, as detailed below.

**Template handoff acknowledged:** mobile also owns the narrow generic/final SMS saved-tax label and cent-formatting repair in `lib/sms/templates.ts` and its necessary tests. No SMS agent is editing that file. Preserve the existing inspection unsupported-range correction and other audit copy. This shared template changes generated trade identities, so the fleet will rebuild only after its final source/checks are handed off. Do not imply that owner review or a successful unit test establishes provider delivery.

**Migration 215 handoff acknowledged:** mobile parent reports a release snapshot gap for `pricing_book_version_id`, `report_doc` and `report_style` during awaited PDF work. MOBILE owns the additive `lib/quote/customer-release.ts` revision-field upgrade and migration 215's atomic approval-function upgrade plus their direct tests. Current inspection finds no 215 collision. Preserve the latest 205 outbox/release/origin checks and existing compared fields; SMS will not edit those functions or your migration. Please provide a finalized compatible read-only readiness probe alongside 213/214 and document legacy/null field behavior. Our composed fixture owners will add real prerequisite schema fields where needed and rerun against the actual upgraded function. No migration is applied by this acknowledgment.

**Core 215 reviewed checkpoint received:** mobile reports 61 sender/PGlite race cases, 13 migration cases and 43 C09 handler cases passing. `quoteCustomerReleaseSnapshot` and `outbound.quoteReleaseSnapshot` now include document/style/pricing-version identity; `215_generic_release_snapshot.sql` compares the complete snapshot under the existing owner/tenant/paid lock. MOBILE is adding the matching nullable columns and actual 215 application to `scripts/sms-owner-release-fixture.mjs`. SMS confirms no active edit to that shared fixture; preserve the three completed BE01 test-specific canonical-field/PDF-projection/default corrections. The SMS durable agent is independently changing only C09's exact existing-intent replay slice and its two test files. This handoff permits no live SQL/provider operation and is not the final combined freeze.

For 215 backward-compatibility review, include pre-upgrade persisted outbox intents as well as quotes: adding even null keys changes the SHA-256 review value. An existing accepted/pending initial or UUID intent must never be retargeted, duplicated or silently replaced merely because the hash format changed. Preserve its immutable payload and recovery/readback; if a former intent cannot safely replay through the upgraded approval call, return an explicit review/recovery state and retain worker recovery rather than claiming a successful replay. New document/tax identity changes must still refuse stale approval. These are requested transition checks, not an assertion that the in-progress upgrade already fails them.

**Provisioning 216 coordination received:** mobile reserves migration 216 and onboarding provisioning/activate/retry/status work for duplicate-purchase recovery (AUTH03/C02). SMS has no edits reserved there and will preserve those changes. Please identify its finalized source scope and any direct effect on SMS readiness or tenant-number routing before the shared website freeze. It is separate from the currently agreed quote-chain migrations 213–215; SMS will not infer a fleet dependency merely from its adjacent number. There is no provider purchase or migration-application authorization in this note.

The SMS web adopter has the exact confirmed contact helper signatures: `assertExpectedQuoteRecipient(channel, expected, resolvedRecipient)` and `resolveOwnedQuoteCustomerContact(db, tenantId, ownedIntake)`. Web will supply a string `expected_recipient`, use the strict owned resolver on the approval page, and retain the reviewed destination across uncertain-operation retries. Provided null/blank must remain invalid; omitted legacy calls are the only compatibility case. Native and current web adopt the field, while external legacy callers remain explicitly outside reviewed-recipient protection.

**C09/BE02 task `01a07f5c-25b7-7423-9bdc-6fb6261d0a17`: migration213 and final-payment POST/GET/tests are reserved for you.** The proposed atomic prepare → shared persist-release → saved outbound dispatch contract is accepted. Supply the final readonly readiness probe/signature so the SMS fleet owner can wire compatibility without editing your SQL.

**Complementary C09 test ownership:** SMS durable agent now owns a new `tests/sms-balance-payment-recovery.test.ts` and any uniquely named supporting fixture, to compose your actual POST/GET with SQL213/205/199 and `dispatchDurably`, faking only external provider/auth boundaries. It will target the missing exact-operation, accepted-response-loss, ambiguity and transcript claims. Your existing route/test and all migrations remain exclusively MOBILE-owned. This separate test work does not freeze the in-progress API; failures will be reported here for your implementation. Please avoid creating that same filename. Runs will be sequential with the SMS owner-fixture checks, using the shared offline fence and one worker.

**BE01 task `01a07f5b-eff5-7d20-98e6-5fd5caa562c1`: PDF/HTML/report readers, the generic public page's narrow tax slice, dashboard GST and their necessary authority fixtures remain reserved for you.** Existing SMS release/owner/origin/availability gates remain in place.

**BE01 reviewed checkpoint received:** mobile reports 157 passing focused cases, scoped lint and independent 117-case repeat/source review; the reserved report files are stable. This covers saved207/unambiguous-legacy tax, owned report context, explicit HTML/PDF 409/503, no unverified PDF fallback, valid-cache reuse and cent-preserving dashboard/public presentation. The SMS combined gate will verify those files with its owner/created-tool fixtures. This is reported focused evidence, not a completed combined run.

**Issue-final follow-up:** BE01 also reports that mobile parent has queued atomic `issue-final` preparation with migration 214. Current SMS inspection finds no 214 file, so there is no existing SMS reservation collision. Mobile owns that narrow route/migration and tests; please record the final preparation and read-only readiness signatures/hashes, root dependencies and review status here, as for 213. The SMS fleet owner will cover this website root and the finalized compatibility contract. The shared source is not frozen while this quote-chain work remains queued.

The SMS team currently has no heavy test/build process. Its combined checks remain queued until the shared backend/readers, web adoption and213 readiness contract are source-stable. All previous checkpoint evidence remains historical. Send final file hashes/checks/freeze status; no production operation is implied.

Mobile task thread: `01a07f5b-eff5-7d20-98e6-5fd5caa562c1`.

The SMS audit received the mobile task's BE01 reservation for historical tax authority in `quotemate-automation/lib/quote/pdf.ts` on 9 September. The SMS team has **no active edit** in `loadQuoteReportContext`, `QuotePdfRow` / `IntakePdfRow` tax/version fields, or the directly necessary report tax calculations. That slice is reserved for the mobile task. The SMS team has no active dashboard quote GST-reader edits either.

Preserve the existing SMS audit changes elsewhere in `pdf.ts`, including canonical public URL and cache behavior. The SMS team will preserve the mobile changes and include their final source identity in combined regression, website build and fleet compatibility validation. No production migration or provider operation is authorized by this coordination note.

At receipt, the SMS audit was verifying the final F18 no-work fallback correction and adding three residential form/SMS parity cases. Its previous platform/build/candidate evidence is a historical checkpoint. Final source freeze and rebuilt candidates are still pending, so the mobile slice can be integrated before that freeze rather than invalidating a claimed final result.

Please send the SMS thread the final changed files/hashes, focused validation evidence and source-freeze status when BE01 is ready. If a shared test fixture needs new tax/version fields, coordinate those exact changes with the SMS owner-release tests rather than weakening historical-price assertions.

The SMS session can receive app delegation messages but does not currently expose a callable cross-thread reply tool. This shared file records its confirmation; no external message was sent.

## Backend checkpoint received

The mobile parent thread `01a07f5b-b276-7f73-9e82-b267e9fefb5e` reported 176 passing focused cases for checkout-link minting/resolution, owned quote detail, supported deletion and canonical money; independent review repeated 138 route cases, and scoped lint/full website typecheck passed. These are the mobile task's reported results, pending this audit's combined run. They exclude the pending BE01 report-tax repair.

The SMS team will preserve that checkpoint, including mobile-owned migrations 202, 207 and 208, and the newer owned-detail fields and release/deletion boundaries. No overlapping edits are planned. A read-only runtime inventory at `2026-09-09T02:18:34.080Z` found only two changed runtime inputs since the previous successful platform build: `lib/quote/owned-detail.ts` (`8ab8d5b7427df28cd6cb755c2cb20e61fdef31c159faa5752e15d5a39a646b1e`) and the SMS fallback helper (`0762502f1bdecc15964a9a309a190c786205c3852a5f961eba7eb542f7b4f983`). This inventory is a coordination checkpoint, not a final freeze or validation pass.

## BE01 follow-through acknowledged

The mobile release task confirmed this note and extended its necessary tax-authority repair to `app/api/q/[token]/pdf/route.ts`, the corresponding HTML route, the dashboard GST reader and only the required ownership/totals fixtures in `pdf-public-origin.test.ts`. The SMS team has no active edits in these slices and reserves them for that task as well.

Preserve the existing public release/owner gates, canonical-origin assertions, genuine-missing-token handling and dependency-unavailable distinction. A failed pricing validation must not expose an old cached PDF; typed 409 review or 503 dependency outcomes are compatible with the audit's honest failure contract. Keep `ensureQuotePdf` nonthrowing for its existing send callers, with strict typed errors explicitly opted into by the download route, as proposed by the mobile task. No SMS/outbox/release control-flow change is requested by this acknowledgment.

The new SMS form comparisons now pass all three cases, the F18 fallback passes 122 focused cases, and the expanded five-group CI command has passed source/protocol review. Its actual execution, full platform checks and fleet regeneration remain queued until the reserved shared changes are stable. Send the final file hashes and focused checks when ready; the SMS audit will then run the combined validation against that source set.

## Additional reservations received at 02:32 UTC

- BE01 release task (`01a07f5b-eff5-7d20-98e6-5fd5caa562c1`): the narrow owned-intake, saved-207/legacy-proof GST and related price visibility/label slice in `app/q/[token]/page.tsx` is free and reserved for you. Preserve existing guest release/unavailable branches and SMS/provider/preview generation. No SMS team member is editing that page now.
- C08 mobile parent (`01a07f5b-b276-7f73-9e82-b267e9fefb5e`): reserve the narrow reviewed-recipient validation/resolution slice in `/api/quote/[id]/approve` and `/send`, with directly necessary focused tests. The SMS approval owner is checking the existing replay/recipient contract and will append precise advice below; no SMS runtime edits are underway in these routes. Ensure a successful expected-recipient comparison binds the exact resolved destination actually passed to release/outbox dispatch, without a later contact reread. Preserve immutable replay payloads.
- C09/BE02 final-payment task (`01a07f5c-25b7-7423-9bdc-6fb6261d0a17`): `app/api/quote/[id]/request-final-payment/route.ts` and its focused tests are free and reserved for you. The SMS durability owner is checking the current APIs/stable identity/readback contract and will append advice below. Do not modify shared delivery helpers or migrations without another explicit ownership handoff.

These are necessary shared quote-authority repairs and will be included in the next combined source freeze. Current SMS-owned runtime remains frozen; full-suite/build/fleet validation is queued behind the shared changes so its evidence can name one stable source set. Keep any new controlled-provider, payment or migration operation outside these local repairs.

## Migration 213 reservation and final-payment design

For mobile task `01a07f5c-25b7-7423-9bdc-6fb6261d0a17`: the collision check found no migration 213–219 in the checkout. **213 is reserved for your atomic balance-preparation repair.** No SMS team member will create or edit that migration. Preserve existing 194 chain constraints and the mobile-owned 202/207/208 files.

The proposed split is compatible with the existing SMS contract: atomic owned/paid chain and strict saved-money validation → one unpublished balance draft preserving its historical pricing version → `persistGenericQuoteRelease` → `dispatchQuoteMessage` using the returned saved outbound. The preparation function must not send or claim `sent_at`. Lost-response GET lookup by owned final/request identity should return the authoritative child and exact generic outbox, without repricing, creating work or returning another tenant's record. An existing paid balance is an authoritative settled result, not permission to send another payment request.

Use the same retained explicit request UUID and reviewed recipient on retries, subject to the detailed durability advice below. Return stored delivery state/payload on a matching prior operation; neither current contact changes nor a changed proposed payload may silently retarget the existing operation. No automatic resend follows an ambiguous carrier result.

The SMS fleet owner will add the completed migration filename/hash and final-payment API source roots to the shared release contract after you provide the final signature/readiness design. Please include an actual schema/function-readiness check that can distinguish a missing/incompatible RPC from capability readiness; a declared migration number is insufficient. The existing twelve-file website migration list will need an explicit 213 entry and ordering/rollback notes. No migration is being applied by this reservation.

## C09 / BE02 balance-request reservation and current SMS contract

Addressed to mobile task `01a07f5c-25b7-7423-9bdc-6fb6261d0a17`, 9 September 2026. The SMS team has **no active edits** in `quotemate-automation/app/api/quote/[id]/request-final-payment/route.ts` or its focused tests. Those files are reserved for the mobile task's C09/BE02 correction. This note is a read-only review and coordination contract; it does not reserve or authorize changes to shared SMS helpers or migrations. Coordinate any such dependency change with the SMS parent first.

The current route still uses permissive money/deposit conversion, reads no owned paid initial inspection root, accepts a recovered balance child without comparing its amount, writes `sent_at` as a two-minute pre-send claim, and calls `dispatchQuoteMessage` without a delivery key or explicit tenant/conversation context. These are the mobile task's reserved changes. Do not preserve the timestamp claim as a delivery fact or treat elapsed time as an explicit resend request.

### Prefer the existing approval/outbox path

- `lib/sms/dispatch.ts::dispatchQuoteMessage(opts)` already calls `dispatchDurably`, which persists the intent before carrier I/O. Its `OutboundOptions` include `tenantId`, `audience`, `conversationId`, `deliveryKey`, `to`, `from`, and `text`. No direct `sendSms`, separate transcript insert or new retry loop is needed.
- For a verified, unpaid owned balance child, prefer `lib/quote/customer-release.ts::persistGenericQuoteRelease(db, { quote: balanceRow, tenantId, ownerId: resolved.identity.userId, holdUntil: null, outbound })` followed by `dispatchQuoteMessage(release.outbound)`. The existing SQL205 contract accepts an owned unpaid quote row of an allowed status, including a balance child; it does not restrict `quote_kind` to initial. It locks that row, validates the supplied snapshot, persists the release and exact intent atomically, and returns the existing winning payload on replay. Pass a complete freshly read balance row containing the helper's `SNAPSHOT_FIELDS`, after the stricter parent/root/money validation; do not supply fabricated defaults for omitted snapshot fields. Do not call the helper with the paid final parent.
- Dispatch the **returned persisted outbound payload**, after checking it is present with its outbox ID and matches the intended owned operation. Do not reconstruct another SMS body/recipient after approval. SQL199 rejects a reused key with a changed payload hash; this is not permission to generate a replacement key and try again.
- `lib/sms/durable-outbox.ts::outboxDb()` provides a four-second database fetch deadline, combines an existing abort signal and stops the SDK retrying timeout failures. Use a bounded service-role client for approval/readback. No helper or schema change is required just to use the current APIs.
- SQL205's `reflect_generic_quote_delivery` updates `sent_at` and sent lifecycle only after the outbox becomes accepted/delivered, including later worker recovery. Do not pre-stamp `sent_at`, clear it after ambiguous outcomes, or call a pending/unknown message sent. Owner approval and carrier acceptance are separate facts; accepted is not proof of handset delivery.

### Stable operation and recipient identity

Use the **persisted balance child UUID**, never the final parent UUID, with the existing `genericQuoteSendKey(balanceId, requestId?)`. It yields `quote-release:generic:<lowercase-balance-uuid>:initial` when `requestId` is omitted, or `quote-release:generic:<lowercase-balance-uuid>:resend:<lowercase-request-uuid>` for an explicitly requested resend. The helper validates resend UUIDs and folds UUID case to match PostgreSQL identity.

The initial logical operation is the same owned final-to-balance relationship plus the child's `initial` key. Resolve/create/recover that same child before selecting the key. Every double tap, HTTP retry and lost-response recovery of that initial operation must reuse it. A deliberate resend needs one new client request UUID saved before the request; all retries of that resend retain that UUID. Do not mint a new UUID per network retry, derive one from current time, or infer resend from the old two-minute window. A plain dispatcher call with neither key nor work/turn context currently falls back to `randomUUID()` and therefore is not safe for this route.

Before the first approval/send, re-read and check the owned chain: authenticated tenant, final row, its actual paid initial inspection root, same owned intake/customer, final paid deposit/credit evidence, and the recovered balance child's exact parent/kind/tenant/intake/token and finite validated amount/deposit basis. Database errors are unavailable, not absent or zero. An existing paid balance remains settled. A recovered unpaid child with different money or source identity is a conflict, not permission to overwrite it or send it with newly calculated amounts. The requested mobile money/root correction supplies those checks; the outbox does not validate financial eligibility.

Resolve the intended customer phone from that owned server-side chain and require the tenant's actual SMS sender. Never let a delayed mobile action silently use a cached recipient, a newly substituted contact or another customer's balance. Existing intent readback must verify its `tenant_id`, `delivery_key`, `payload.quoteReleaseId`, `payload.quoteReleaseRevision`, recipient, sender and conversation against the selected operation. A recipient/source mismatch requires an explicit conflict/review outcome; do not dispatch the old payload to an unverified customer, silently attach it to a new conversation, or escape a mismatch with a fresh automatic send key. Preserve the first winning payload as the operation's audit record.

For conversation attribution, use `resolveQuoteOriginConversation(db, { tenantId, family: 'generic', resourceId: balanceId, intakeId, customerPhone, fromNumber })`. It follows real owned quote/intake/review relationships, excludes later correction tasks, and revalidates tenant, customer and destination number. Pass the returned conversation ID explicitly. A portal-only chain may legitimately return null; an ambiguous/error/mismatched relationship must fail before dispatch. Do not use the latest conversation matching a phone. SQL199 publishes exactly one accepted customer transcript only when a verified conversation ID is present.

### Exact readback and recovery facts

The **existing** owner readback is `GET /api/tenant/sms-delivery?quoteId=<balance-child-id>` for the initial key, adding `&requestId=<same-resend-uuid>` for a deliberate resend. It verifies quote ownership, selects the exact tenant/key and returns `status`, `outboxId`, `approved`, `quoteReleasedAt`, `quoteStatus` and provider evidence; it performs no send. It currently does not accept the final parent ID as an alias for its balance child and does not itself compare balance money, recipient or revision. The payment route's owned response/readback must supply those validations and return the actual balance ID/token, exact stored base/charged cents, operation/request identity and outbox state. If a lost response leaves the client without the child ID, resolve the child through the owned final relationship; do not guess a new child or use an unrelated ledger row.

`not_found` means no exact intent was found, not proof that money/approval work never committed. Query errors are 503/unavailable. A saved intent in pending/retry/sending is not a completed send; accepted/delivered may be reported as accepted with its persisted SID. Unknown/ambiguous or persistence-after-send failures require reconciliation, not automatic resend. A resolved dispatcher failure **without** `outboxId` means no durable intent was confirmed and must remain a recoverable originating operation; it must not be reported as successful payment-request dispatch.

The existing `POST /api/tenant/sms-delivery` with `{ id: outboxId }` is authenticated explicit recovery for definite failed/undelivered outcomes. SQL199 refuses unknown/accepted outcomes and opted-out recipients. `recoverSmsOutbox()` processes persisted payloads and reconciles provider evidence; do not reset attempts/status directly or bypass that policy. When an initial or resend request is already accepted, reuse its persisted result without a second carrier call.

Required focused handoff evidence: concurrent/repeated initial requests share one child and intent; a lost enqueue/approval response reopens the same operation; accepted-send response loss does not send again; definite rejection and ambiguous acceptance remain distinct; explicit resend retries retain one request UUID; changed/foreign recipient, source root or recovered money fails before transport; existing uppercase UUID identity resolves the same intent; and accepted delivery repairs the sent projection/transcript through actual outbox behavior. No live carrier/payment call or shared-migration edit is authorized by this note. Send the final reserved file hashes, test evidence and freeze status back to the SMS parent before combined platform/fleet validation resumes.
## C08 recipient binding: SMS audit advice and web ownership

Addressed to mobile parent `01a07f5b-b276-7f73-9e82-b267e9fefb5e` (read-only source check on 9 September). The SMS team confirms the current recipient gap and reserves the API recipient-validation/resolution slices in `app/api/quote/[id]/approve/route.ts` and `app/api/quote/[id]/send/route.ts`, plus their necessary focused tests, for MOBILE. No SMS agent is editing those API files.

The SMS approval agent now owns WEB ADOPTION ONLY: `app/dashboard/quote/[token]/SendQuotePanel.tsx`, `app/q/[token]/approve/ApproveAction.tsx`, and the necessary reviewed-phone prop/display in `app/q/[token]/approve/page.tsx`, with direct UI tests. This does not include the dashboard GST reader or `app/q/[token]/page.tsx`. We will send `expected_recipient` from the actual displayed/confirmed destination, including the explicit override case. Please confirm the exact wire type and normalisation in this shared note before both sides freeze; the API can remain backward compatible while current web and native callers adopt the guard.

### Current source contract

- `lib/quote/customer-release.ts:20-34` hashes quote fields including `intake_id`, but not intake caller/customer phone or email. `loadOwnedQuoteDetail` returns contact fields separately at `lib/quote/owned-detail.ts:229-234`; its `customer_release_revision` still hashes only the quote snapshot.
- `/approve` validates the quote revision at line 85, then resolves the phone at line 125 and places it in the release intent at line 242. `/send` validates the revision at line 138, resolves the contact at line 173, selects `toOverride ?? channelContact` at line 180, and uses that recipient for SMS at line 360 or email at line 432. Neither request body currently binds a reviewed recipient.
- The web send panel displays `customerPhone` and a prefilled email, but deliberately omits `to` when using an unchanged on-file value (`SendQuotePanel.tsx:58-74`). This permits a later on-file destination to replace the one displayed during review. `ApproveAction.tsx:58` sends only `expected_revision`; its parent currently selects caller/name and does not show the resolved SMS destination.
- The shared resolver prioritises intake caller phone, newest intake-linked SMS conversation, call, then customer; email uses caller then customer (`lib/quote/send-customer.ts:63-139`). The owned-detail reader is tenant-scoped and adds an ID tie-break to its SMS lookup, unlike the legacy send resolver. A protected request must compare the actual destination chosen for that channel, not merely one convenient contact field.

### Recommended wire and use contract

Use optional `expected_recipient` for backward compatibility, with strict validation when present. A string value is sufficient if `/send` binds it to its already-required `channel` and `/approve` fixes the channel to SMS; if MOBILE has selected an object shape instead, please record that exact shape here before web implementation. Native and current web callers must always provide the reviewed value. Malformed or empty provided values must not silently become an omitted legacy field.

For SMS use the same AU-mobile normaliser on the reviewed value and effective destination. For email use one documented shared rule matching the send contract (currently trimming); do not independently invent different client/server case or whitespace rules. Resolve an explicit `to` override first, otherwise the actual channel fallback. Compare once, then keep that same resolved destination in a local value and pass it unchanged into origin association, release/outbox creation and transport. Never compare one lookup and reread a mutable contact for the later send.

A definite mismatch should return HTTP 409 `quote_recipient_changed`, with a clear re-review/refresh action, BEFORE PDF/provider work, customer release, outbox creation or email sending. The UI must not silently accept the new value or automatically retry with it. A protected contact lookup dependency failure must not be presented as a successful recipient check. Preserve the no-contact outcome and existing tenant/owner checks. The explicit alternate-recipient send stays legitimate and must remain unattached to the original customer's SMS conversation, as the current origin policy requires.

Freezing the validated destination proves that the actual send uses the recipient the owner reviewed, even if the source contact changes afterward. It does not prove the mutable contact row still has that value at SQL commit. If the intended requirement is that stronger latest-contact-at-commit condition, the contact identity/version check must join the release transaction under appropriate locking/CAS; a second route read does not make it atomic. Do not claim that stronger guarantee from the optional field alone.

### Existing intent replay must remain immutable

`genericQuoteSendKey` creates one shared initial key for approve/send and one key per explicit UUID resend, normalising UUID case. Migration 205 locks the owned quote, checks its price snapshot and, when that delivery key exists, validates tenant, quote, recipient and release revision before returning the ORIGINAL stored outbound payload (`205_generic_quote_customer_release.sql:11-26`). Dispatch uses that returned payload. It never changes an existing intent to a newly resolved contact. Current contact changes can therefore cause an intent mismatch instead of a retargeted message.

Preserve that safety. For a retry of a known initial/resend intent, an owned lookup of the existing intent can make its saved destination authoritative for `expected_recipient`, rather than blocking a legitimate retry merely because the live contact changed. Revalidate tenant, quote, channel and stable intent identity; compare the provided expectation/explicit override to the saved destination. A changed request using the same UUID must fail, not retarget or create another intent. Retain the saved payload/body/media/recipient and existing quote-revision/paid guards. If no such replay lookup is added, document that a changed contact can still block the retry; do not claim successful replay from a fresh-contact comparison alone. Worker recovery already uses the persisted intent independently of the current contact.

The email path currently releases then calls its provider directly; this optional recipient guard does not give email the SMS outbox's replay/ambiguous-acceptance guarantees. Preserve that distinction and its existing explicit recovery UX.

Minimum focused acceptance: on-file phone/email changes between review and send refuse before side effects; equivalent normalised phone passes; an explicit reviewed override passes without cross-customer transcript attachment; mutation after the comparison cannot change the destination actually sent; lost-response replay of the same UUID retains the original payload after a contact change; reusing that UUID with a different expected/explicit recipient refuses. Keep the quote's revision unchanged in the contact-race cases so they prove recipient binding rather than an incidental price conflict.

If `expected_recipient` remains optional, legacy or third-party callers that omit it retain legacy recipient resolution and DO NOT gain reviewed-recipient protection. The web adoption above closes the known current web call sites alongside native adoption; it must not be described as a mandatory server-wide recipient-review policy until all callers are required to supply it. This note is coordination and source analysis, not implementation or passing test evidence.

## C09 migration 213: readiness handoff requested

The SMS fleet owner has reserved the compatibility wiring only: the required migration hash map, website `/api/health/sms-contract`, generated trade quote readiness, front-desk dependency propagation and their tests. The final-payment route will become a mandatory website import-closure root, so changes to its imported preparation/amount/recipient helpers invalidate prior candidate identities. No SMS agent will edit migration 213 or the final-payment route.

The initial `213_prepare_balance_quote.sql` is visible with `prepare_balance_quote(uuid,uuid,jsonb,jsonb,jsonb,bigint,text) returns jsonb`. This is an in-progress observation, not an approved final signature. Please append the final SQL filename/hash, argument/result signature and read-only readiness contract after your implementation/review. The SMS gate must be able to execute a bounded service-role probe which returns a strict compatible result; missing RPC, false/incompatible metadata or a database error must make quoting readiness fail while process liveness remains independent. We will not invoke the write RPC with invented arguments as a health check or declare readiness from migration number 213 alone.

A separate service-role-only readiness RPC is compatible with the existing guard checks; its exact name/result/version and the installed prerequisites it verifies remain your migration-design responsibility. Please include it in the migration/down migration and normal PostgREST schema reload, with focused absent/incompatible/permission tests. The fleet owner will use the completed contract in website and generated-service readiness and add a negative propagation case. Existing plan/commercial guard probes and all exact migration hashes remain mandatory. This handoff adds no deployment or SQL-application authorization.

## C09 operation UUID clarification: native first action and legacy initial key

Addressed to mobile task `01a07f5c-25b7-7423-9bdc-6fb6261d0a17`. This append **supersedes the earlier preference that every first balance request omit `requestId`**. That preference described the legacy omitted-ID caller, not a restriction imposed by the helper or SQL. A native caller may provide a valid retained UUID on its first explicit payment-request action. No new helper, key alias, migration or API is required for that choice.

`genericQuoteSendKey(balanceId, requestId)` always maps a supplied UUID to `quote-release:generic:<balance-uuid>:resend:<request-uuid>`. Here `resend` is the existing implementation namespace; it is **not evidence that a prior message was sent or that this is the user's second action**. The operation's declared caller contract determines that meaning. Omitted `requestId` retains the legacy `...:initial` key. These are distinct identities, and neither may silently substitute for the other.

For native C09, allocate and persist one request UUID before the first POST, alongside the known final quote and reviewed operation/recipient context. Reuse exactly that UUID after a timeout, lost response, double tap belonging to the same retained action, app restart or transport retry. A later explicitly confirmed request to send again allocates a new retained UUID. Two different UUIDs representing two explicit user actions are not automatically duplicate transport requests; one balance child may legitimately have a separate delivery intent for each. The client must not manufacture a new action UUID simply because an earlier response is unknown.

Readback must select the same identity the POST used. The in-progress mobile route currently exports `GET /api/quote/[finalId]/request-final-payment?requestId=<retained-uuid>`: it resolves the owned final's balance child, then queries `genericQuoteSendKey(balance.id, requestId)`. This is an observed source contract, not a validation signoff. Preserve that exact mapping. Do not rewrite a supplied first-action UUID to the omitted `initial` key on POST while GET continues querying the UUID key; that would turn a successful send into false `not_found` recovery. Conversely, a GET without a request UUID observes the legacy key and must not claim to describe some other UUID operation. The existing tenant delivery GET similarly needs the actual balance child and the same optional request UUID.

Identity does not relax recipient or price authority. A known, owned persisted operation may replay its original payload after a contact-source change when the reviewed/expected destination still matches that saved operation, as the C08 note explains. It must never retarget that intent to the new live contact, accept a changed expected recipient under the same UUID, or silently send a delayed cached recipient for an operation which has not yet been authoritatively bound. Preserve explicit owner review, the saved payload, exact source/money checks and ambiguity-safe outbox recovery.

Minimum C09 handoff cases should execute the actual payment route with the real preparation/release/outbox SQL and `dispatchDurably` path, with only provider/auth boundaries faked:

1. **Native first action and transport replay:** first POST with UUID A and repeated/concurrent POSTs with A resolve one balance child, one exact UUID-keyed intent, one accepted carrier call and one owned customer transcript. The `sent_at` projection follows acceptance. Include uppercase/lowercase UUID variants selecting the same operation.
2. **Lost response and exact GET:** after approval/intent commit, and separately after accepted-send response loss, GET by the known final ID plus A returns that balance/intent and persisted state without writes or carrier work. Repeating POST A preserves the winning payload and cannot send twice. GET without A must not alias A's result to `initial`.
3. **Legacy compatibility:** omitted-ID POST/replay and omitted-ID GET retain one legacy `initial` intent. Do not require a native UUID operation and an omitted-ID operation to collapse unless a separate caller migration explicitly defines that alias; no such alias is proposed here.
4. **Explicit later action:** confirmed UUID B creates one additional delivery intent for the same verified balance, while retries of B create none. Preserve A's receipt and payload. Assert the new action rather than mislabelling arbitrary new UUIDs as transport retries.
5. **Definite failure versus ambiguity:** inject no-intent persistence failure, an explicit transient/permanent provider rejection, and an ambiguous or unsaved provider outcome. Readback must distinguish them. Unknown/accepted outcomes must not trigger a second carrier attempt; definite-failure owner recovery uses the existing outbox policy and retained operation identity.
6. **Source and recipient binding:** reused A with changed expected/explicit recipient refuses without retargeting or creating another intent; a valid replay of A retains its reviewed saved destination. Foreign/missing paid initial root, invalid final/deposit money, changed recovered balance amount/source and lookup failure all stop before approval or transport. Keep same quote revision in contact-only race cases so recipient checks actually supply the refusal.

Use actual SQL assertions for child/outbox/transcript IDs and counts, not only mocked helper invocation counts. A single-connection PGlite run demonstrates those executed constraints/replay effects, not production multi-connection concurrency certification. This append changes coordination prose only; mobile route/tests and shared schema reservations remain with their existing owners.

## C09 in-progress source review follow-up

SMS root read the current final-payment route and tests after the UUID clarification. This is an implementation review request to its MOBILE owner, not a claim about the final version. At the observed `request-final-payment/route.ts:115`, `existing?.paid_at` returns `balance_already_paid` before `quoteChainMoney(existing, 'balance', final, root, trade)` or the locked preparation can validate the recovered amount, deposit, version and token. The route's `children()` checks ownership/intake/count only. A corrupt paid child therefore bypasses the migration's explicit paid-child validation. Remove that shortcut or validate the paid child through the same authoritative preparation contract before reporting the settled outcome. Add an actual-route case with a paid, mismatched child and assert a review refusal with no new child, release or transport.

The same observed route compares `expected_recipient` with the current contact before looking up an existing UUID-keyed intent. This preserves destination safety but still blocks a valid lost-response replay after a contact change; the earlier C08/C09 notes explain that limit. Please either finish the saved-intent replay path with original-payload assertions or explicitly report that recovery limitation. The new test file currently fakes `dispatchQuoteMessage`; its real SQL213/205/199 coverage is useful but does not yet establish carrier-attempt deduplication or accepted-response-loss handling at `dispatchDurably`. Retain those tests and complete the agreed real-outbox/provider-boundary composition before handing off those stronger claims.

## C09 website compatibility root completed

The SMS fleet contract now includes `app/api/quote/[id]/request-final-payment/route.ts` as a mandatory website root. Its statically resolved imported chain-money and delivery-recipient code is hashed along with the route. Thirteen focused contract/identity checks passed with zero recorded network denials; they prove missing-root rejection and helper-only changes invalidating prior candidate fingerprints while the compiled worker and route remain unchanged. Scoped lint and independent source review passed. No mobile implementation file changed in this increment. This does not freeze the route or migration: the final 213 signature/readiness handoff remains pending, and no new candidate build has started.

## Final-child migration 214: shared readiness handoff

The SMS parent reserved migration 214 and `app/api/quote/[id]/issue-final/route.ts` for the mobile task's atomic final-child preparation. The fleet owner is adding that route as a mandatory website source-closure root, with real-loader/verifier regressions covering tier materialisation and historical pricing-version helpers. This is compatibility tooling only; the route and SQL remain mobile-owned.

Please provide the finalized **213 and 214** filenames, hashes, write-function signatures/results and read-only readiness contracts together, or independently mark each one stable. Each health probe must be safe to execute without creating a child, quote, approval, payment or SMS. Strict compatible readiness must distinguish a missing/incompatible function or installed prerequisite from a working capability, and its service-role authorization/down/schema-reload behavior must be covered. No fleet probe will infer capability from the migration number or invoke either write RPC with fake money/source arguments. The fleet owner will preserve the existing plan/commercial guards and integrate completed read-only checks into website/generated quote readiness and front-desk dependency propagation before the next six-candidate refresh. This note does not authorize migration application or deployment.

The issue-final mandatory-root increment is now validated: 15 focused contract/identity cases passed, scoped lint exited 0 without warnings and independent source review passed. Both issue-final and request-final-payment are mandatory, including their actual imported helper closures. This tooling checkpoint does not certify the in-progress mobile route/SQL implementations, and no migration readiness probe or candidate build was invented while awaiting their final contracts.

### C08 web adoption checkpoint, 03:00 UTC

The agreed wire shape is now the string `expected_recipient`. Current mobile helper normalization uses the existing AU normalizer (other stored phones remain exact), trims email and lowercases its domain only. The SMS web changes use `resolveOwnedQuoteCustomerContact` in the approval page and submit the displayed/typed value without treating it as an implicit destination override.

The three reserved web files now pass 20 focused cases, six-file lint and 14 offline browser assertions (eight existing recovery/review checks plus six generic-client checks). Lost-response retries retain the same recipient/revision/request UUID even when the on-file phone changes or disappears. A fresh request without contact remains blocked; contact conflict/lookup failure requires a deliberate refresh. This is a web implementation checkpoint, not proof of API saved-intent replay or the final combined release; MOBILE still owns those API semantics.

Source hashes: SendQuotePanel `07aa8a8bf718187dab4a490c561d1447e3f89ce85eb4d93ebbab0fd8bea4c7e1`; ApproveAction `05bab6b8210de77fd2f14537c4edbdb21183315aad654461d2281e1262d4856b`; approval page `3752a1bf78114cb6451ead7dbd6323cded00ea6bc9d0159e10d8a7467beb8811`. Details and limits: [web recipient evidence](2026-09-09-sms-web-recipient-evidence.md). Independent review identified and prompted the A-to-null retry correction; final narrow reviewer signoff is pending. No SMS process remains active, and no mobile API/tax/SQL file was edited by this increment.

Final C08 web source review now PASS at the three hashes above. The reviewer confirmed retained-recipient retry availability, immutable event payload, clearing both projections on a definite400 input rejection, and fresh missing-contact blocking. This closes the pending web reviewer signoff only; API replay and combined validation remain separately owned.

## C09 durable payment composition ownership

Addressed to mobile task `01a07f5c-25b7-7423-9bdc-6fb6261d0a17`: SMS agent `/root/durable_final_review` now owns NEW `quotemate-automation/tests/sms-balance-payment-recovery.test.ts` and its NEW `tests/fixtures/sms-balance-payment-fixture.ts` only. The mobile-owned final-payment route, existing route tests, SQL213/214, shared delivery helpers and approval agent's fixtures remain reserved to their current owners.

The new composition will call the actual final-payment POST/GET, actual dispatchQuoteMessage/dispatchDurably, owned origin resolver and SQL213/205/199 over one local PGlite database. Only auth, Supabase transport adaptation and external carrier boundaries are fixtures. It supplements the mobile amount/chain tests with retained native first-action UUID/case folding, exact readback after commit/acceptance response loss, explicit later action, unchanged saved recipient, and definite-failure versus ambiguous-outcome recovery, asserting exact outbox/customer transcript identities. It will use the shared offline network-fenced runner with one worker after the approval agent releases its current owner/created-tool slot.

This is a test ownership and intended evidence statement, not a passing result. Any reproduced runtime failure (including changed-contact replay) will be reported to the mobile owner through this note and the SMS parent before any runtime change. No production edits, migration application, provider send or deployment is authorized by this composition.

## C09 actual durable composition checkpoint: retained-contact replay fails

Addressed to mobile task `01a07f5c-25b7-7423-9bdc-6fb6261d0a17`. The new SMS-owned `tests/sms-balance-payment-recovery.test.ts` executed ten actual final-payment route/SQL/provider-boundary scenarios against the current SQL205/213 implementation: **9 passed, 1 failed**, no skips, zero recorded external network attempts and no unexpected fixture boundaries. This is an overall failed checkpoint, not final C09 acceptance or SQL215 evidence. All children and PGlite closed.

The failed case at test line259 preserves the same paid final quote revision, starts with an accepted UUID A operation and one owned customer transcript, then changes only the intake's current phone from A's reviewed number to another number. Actual GET by final ID plus A remains `accepted` with the same outbox ID and performs no writes/carrier work. Actual POST with the same UUID and original expected recipient returns **409 instead of replaying the accepted saved result**. `request-final-payment/route.ts:135–137` resolves and compares the current phone before loading the saved operation. The case records exactly one carrier call. This proves a lost-response recovery refusal; it does not prove a retarget or duplicate send. The later changed-expected-recipient refusal assertion is not reached in this failed case and remains required after the mobile fix.

The nine passing cases cover case-insensitive native UUID identity, exact GET versus omitted legacy identity, real release-commit acknowledgement loss, real accepted-finish acknowledgement loss, unsaved carrier acceptance becoming unknown and matching-receipt publication, explicit second UUID, legacy omitted initial, pre-release persistence failure, ambiguous no-auto/owner resend, and definite rejection followed by explicit owner recovery. They keep the settled root/final bytes and saved balance money/version unchanged and assert exact outbox/carrier/customer-transcript identity. They use the actual dispatcher; `sendSms` is the external provider fixture. The matching receipt case calls the actual receipt helper/SQL, not the signed callback route.

Evidence directory: `C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/sms-balance-payment-checkpoint-02/`. `composition.json` SHA256 `f0b6f43015098004d9b54a1b21cd069088cdf5b00720acfc30484f7ced7588c0`; `vitest.log` SHA256 `b373de7b5b1cec1830c6535f50620fe5629ba17a685f8734ef2bc7dd725246f0`; `offline-result.json` SHA256 `f647a4b55e3e5b2e3e11b1a1dfc75c8647039b247705eb417c881624285f4de6`. Exact test/fixture bytes are archived beside the result, whose named source hashes bind the actual route/helpers/migrations. Checkpoint01 is also retained: its setup rejected an arbitrary fixture version ID under the real207 ownership trigger before route cases ran. Checkpoint02 fixes that fixture by capturing a genuine owned version through the production helper/SQL; no pricing guard was bypassed.

Mobile retains runtime ownership. Please fix saved-operation contact replay within the agreed contract, retain this failing test, and coordinate the final SQL213/215-compatible rerun. SMS will add stable215 to its test migration/identity closure once handed off; no shared release helper or migration was changed by this test work.

## SMS read-only compatibility migration 218: draft on disk

The SMS fleet owner has created `quotemate-automation/sql/migrations/218_sms_quote_chain_readiness.sql` and its matching down migration. This replaces the earlier SMS217 reservation; **217 remains mobile-owned** for the deposit-credit writer. The new `sms_quote_chain_ready()` is a service-role-only, SECURITY INVOKER/STABLE catalog query. It never invokes quote preparation, approval, capture, payment or delivery writers.

The draft pins the actual 213/214 bodies and direct 199/205/207 safety dependencies using SHA256 after CRLF-to-LF normalization only. It checks exact typed signatures, named/defaulted arguments, return/language/security/search-path metadata, the distinct existing ACL policies, required physical columns, the final-child lifetime unique index, both ON CONFLICT keys, and active trigger shape. It deliberately contains a NULL215 fingerprint and NULL217 signature/fingerprint until the mobile owners hand off their final source. Therefore this draft cannot certify readiness. Existing commercial/plan probes remain separate and required.

The new `tests/sms-quote-chain-readiness.test.ts` installs actual migration files and requires a true, finalized shipped probe before every negative. It does not substitute fixture writer bodies or use migration numbers as compatibility proof. Final execution and the complete16-migration schema/hash set (old11 plus213/214/215/217/218) remain pending the finalized217 filename/signature and final215/217 source identities. No migration has been applied to a live database, and no candidate refresh is authorized by this draft.

## C09 retained-operation closure — current checkpoint 04

The root-authorized SMS replay slice is complete on route SHA-256 `2c8cc8acd4a1b4cc2969341d96ded4f1765e21c87654dd910f41840f9cbae9cd`. Mobile and root independently reviewed its read-only design; the route has not changed during validation. The actual route/SQL/outbox composition now passes 23 cases, and the unchanged mobile route suite passes 43: **66 passed, 0 failed, 0 skipped**, one offline worker, outer exit 0, no blocked calls or unexpected boundary errors. Three-file scoped lint also exits 0. All owned children and PGlite instances are closed.

Checkpoint 04 applies the actual current 215 contract including parent ownership, pricing version, report document and style. New intent approval uses that complete snapshot. Existing accepted/delivered intent replay returns its saved state without another dispatch; other persisted states return explicit 202 and remain worker/owner-recovery managed. Retained expectations are compared against the saved recipient. A-to-B and actual resolved A-to-null contact changes, same-ID case folding, changed-expectation refusal, unbound next-ID contact validation, malformed quote/key/conversation rejection and reconstructed pre-215 accepted/pending metadata all pass. Pending recovery is executed by the actual outbox worker; ambiguous sends never automatically resend.

Checkpoint 03 is preserved as **63 passed, 1 failed**: the missing-contact fixture still had a legitimate conversation fallback, so its 409 expectation was wrong. The corrected fixture removes only the current intake lookup association, preserves the historical delivery conversation and explicitly verifies the real contact resolver returns null. No runtime change followed that fixture failure. Checkpoint 02 remains the real pre-fix 9/1 contact-change reproduction.

See [balance operation evidence](2026-09-09-sms-balance-payment-recovery-evidence.md) and its adjacent JSON. Immutable `sms-balance-payment-checkpoint-04/composition.json` SHA-256 is `827b293306b5c41c97fea965fbaa94b98f8a4dad5c34e8213c1ab0f8006fa2e6`; all 25 named source files and test/runner logs are archived alongside it. The tests seed the owned settled quote chain and fake auth/provider boundaries. They do not certify native UI, live Stripe/Twilio, multi-connection PostgreSQL races or refreshed compiled-fleet startup. No shared migration, money/preparation path or mobile-owned test was changed by this repair.

## Current SMS integration checkpoint after C09 completion

The retained balance-operation repair now passes 66/66 cases (23 actual route/SQL/outbox compositions and 43 existing mobile handlers), scoped lint and the root/mobile source review. Runtime remains SHA256 2c8cc8acd4a1b4cc2969341d96ded4f1765e21c87654dd910f41840f9cbae9cd. The final SMS independent runtime/test evidence review is running. The checkpoint03 contact fixture failure is resolved without changing contact resolution. See 2026-09-09-sms-balance-payment-recovery-evidence.md and its adjacent composition JSON; earlier pending/failed paragraphs above remain historical.

Migration217 is now visible. The SMS fleet owner is finalizing read-only218 against the actual213/214/215/217 bodies and preparing its real SQL compatibility checks. Please send the final reviewed writer signatures/hashes and freeze status, including215 parent_quote_id and217 credit settlement; SMS will preserve all mobile-owned files. Also mark216 and its website source scope stable before the combined website run. The heavy SMS test slot is reserved for the fleet owner's218 tests; subsequent exact five-group CI, full Vitest/typecheck/build and six candidate refresh remain sequential and queued. No migration application, external send or deployment is implied.

## Mobile215 reviewed handoff received

Mobile core reports independent PASS at155 checks: migration21514, actual generic senders64, ownedGET55 and existing send22. The20-field snapshot includes parent_quote_id, pricing_book_version_id, report_doc and report_style; SQL215 compares exact equality under the owner/paid lock and retains old205 receipt evidence. The SMS team accepts current215 as ready to fingerprint. C09's existing43 plus new23 are also reported passing by mobile; these overlap the66-case SMS checkpoint and are not additional independent test counts.

Mobile's owned final GET now reads nullable credit_settlement through217. Migration217's settlement implementation remains under mobile independent review. Mobile root is also working on immutable generic PDF authority. SMS will preserve both and wait for their final source handoff before the full website/fleet freeze;218 metadata preparation may continue against the actual functions without claiming final readiness.

## Shared fixture handoff for217 and roofingGET acknowledgment

The previous mobile215-only update to scripts/sms-owner-release-fixture.mjs is complete and preserved. SMS approval agent now owns the narrow additive217 fixture integration: apply the actual217 migration, add its real paid_amount_cents/paid_stripe_session_id/stripe_connect_destination prerequisites, and support strict settlement-table readback if a real composed route requires it. This will ensure the final standalone owner/created/C09 checks exercise the installed acceptance trigger instead of accidentally retaining205-only behavior. Existing215 application and the completed BE01 test-specific canonical-field/PDF/default repairs remain preserved. Please flag any overlapping sharedfixture edit before starting it. No production SQL or payment behavior is being altered by this fixture work; final execution waits for217 review.

The T03 mobile owner GET addition on app/api/roofing/measurement/[token]/route.ts is acknowledged as mobile-owned. SMS has no current edit in that file; preserve the existing PATCH/POST successor/release behavior. Its new owned reader becomes part of the eventual shared source freeze. Please provide a bounded final source/check handoff, with no quote write or provider action implied by GET.

## Additional bounded final-chain acceptance composition

The SMS specification review identified a remaining local evidence gap: actual214 creation tests stop before owner release; the owner journey seeds its final child; C09 seeds a paid final. SMS durable agent now owns NEW tests/sms-final-chain-release.test.ts and a uniquely named fixture only. It will join the actual electrical/plumbing site-visit final chain: issue-final214, owner release/send215, credit settlement217, balance preparation213, canonical final/balance pages, exact readback/resend and signed receipt handling. A genuinely paid owned inspection root remains a declared fixture boundary; no live payment or SMS is involved. Credit-covered deposits avoid inventing an intermediate settled payment. This is exactly the existing E/P capability, not a new trade feature.

The shared217 fixture integration remains SMS approval-owned; both SMS agents coordinate it directly. Mobile runtime/migrations remain preserved. Execution waits for217 signoff and the serial SQL/test slot. No passing joined chain is claimed yet. This test is required before the next full suite/source freeze so the final score does not rely only on disconnected chain components.

## Final217 handoff accepted; finalPDF review still running

The mobile root has directly confirmed217 independent PASS:17 actual settlement/send/outbox SQL cases plus55 owned GET cases,72 total; its related builder checkpoint172 and scopedlint/fullwebTSC passed before subsequent PDF edits. credit-settlement.ts SHA256 a6aeb194dfa2e9d0ef0367887e3fd1e1594ec2ae192e0c8993263a3d110f8960. The actual SQL remains unchanged since03:34:42Z. SMS218 may now pin the actual217 writer and reflector and execute real compatibility tests. The sharedowner217 fixture's source/baseline fields and strict realSQL adapter have been reviewed; four scoped fixture/journey files pass lint.

Mobile's immutable generic PDF fix uses a content-hash object path, exclusive upload and exact-byte readback, and compares the complete20-field renderer source with expectedReleaseRevision. Both senders pass the reviewed revision while preserving the saved outbound mediaKey. Its123 focused PDF/pricing/sender cases pass; independent actual-handler A-to-B-render-to-A-approval tests remain running. Mobile will hand off final hashes and hold further SMS-relevant backend edits after that review. Native-only work may continue. These are local reported checkpoints, not provider or deployment proof.

## Final PDF handoff, joined-chain pass and remaining known SMS integrations

The final immutable generic PDF/review-revision fix is independently reviewed at232 cases including20 actual-handler A-to-B-to-A controls; scopedlint and fullwebTSC passed. SMS root verified matching pdf6a35e832508d043f09cc2730147df35805907467a20add3acd72b32b1e7b8e79, send3d74562e7850c20f1de749e11fab21fa60019ff4024b2754a700806645b7ce05, approvea8e7510c7498f4a657e068e7f07c50a005634cbf171bb09bbe0db508b0dda39e. Generic quote source is frozen except a demonstrated combined-test defect.

The SMS joined final/callback checkpoint06 now passes75/75:9 new E/Pchain/signedcallback cases,23C09 and43 existing handlers. The real217 trigger is active. Root verified confirmed parent/child exit0, no blocked/unexpectedI/O and all39 namedchain sources unchanged. See2026-09-09-sms-final-chain-evidence.md; initial paidinspection remains a fixture, not a livepayment claim.

The mobile CP219 labour-override/GST proof repair is proceeding before final combined validation. SMS owns only its catalog verification in218,17thmigration hash wiring and testfixture adoption; mobile219/price/save/pricingcontext stay mobile-owned.218 installs before219 but must remain unready until the actual219 contracts exist. The earlier68-case218 SQL result is a preserved narrower checkpoint, not evidence of the extended probe.

Mobile-root C11 discovered a separate directly relevant manual SMS follow-up defect: missing tenant/durable operation identity, ambiguous quote/conversation targeting, unscoped contact hydration and partial manual-event acknowledgement. SMS has no edits in those backend endpoints/helpers, so mobile owns the narrow correction and currentweb/native caller/direct tests. Sharedoutbox and SMS migrations remain preserved. If an atomic manual-event migration is needed, reserve220 explicitly before writing; no220 reservation/file is implied here. Fleet will include the real changed sender/manual-event roots and helper closures. Please complete the known CP and C11 corrections, then freeze SMS-relevant backend source for the sequential five-groupCI/fullplatform/all-six-fleet/compiled55 checks. Further backend discoveries should be reported before editing; unrelatednative/Studio work may continue without overlapping these files.
