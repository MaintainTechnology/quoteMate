# Offline owner approval, public quote access and resend evidence

**PASS: 93/93 owner tests against actual221 and the specialist immutable-PDF source**, including all eleven composed cases, in `standalone-ci-five-group-final-05/website-composed-tests/owner-release`. The child exited0 and confirmed shutdown; there were no failed/skipped cases, unexpected operations or blocked network calls. The current [composition JSON](2026-09-09-sms-owner-release-journeys.json) SHA256 is `607a95ffbf93ac523bad07261d194ac1f0a011dba2df5bdc29a5f914ccc4e753`, with56 named source inputs and30 accepted fixture-carrier calls. The [exact five-group run](2026-09-09-sms-standalone-ci-evidence.md) passes121/121 and archives334 matching before/after/current inputs. Four edited fixture/test files pass scoped zero-warning lint.

The commercial held quote now starts from raw unpriced takeoff data and explicit owned tenant rates. Actual correction GET/POST and221 SQL establish the observed revision and confirmation; actual219 pricing supplies the BOM/proof/exact generation used by the real rich-run approval guard. The test independently fixes the expected result at **A$2,481.95** for100m²/two coats and its specified rates, and requires A$2,482 in the rounded public display. It does not echo a route result as its expected price or manually seed a confirmed/priced extraction. The original owner/review/replay/tenant/outbox and forbidden-renderer assertions remain active.

The initial221 run04 passed87/93 owner tests, including all eleven composed journeys, but six separate paint/solar PDF-gate cases still used superseded flat cache keys. Their real cache helpers correctly refused those keys and reached unavailable fixture-renderer inputs. The [narrow seed repair and retained failure](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/owner-pdf-cache-fixture-221-01/fixture-repair.json) preserve exact original PDF bytes under the real token/revision/origin/SHA256 path and reject downloads of any other key. Independent review confirmed the entire14-case assertion block is byte-identical, with auth/release/read-error behavior and renderer prohibition unchanged. The complete05 rerun passes all93; no runtime guard was relaxed.

This is local composition evidence. Authentication, provider acceptance and storage are fixtures; browser behavior, production RLS/PostgREST, multiconnection timing and actual delivery remain separately bounded. The 221 server handoff and adapter signatures have [independent source review](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/221-composition-handoff-independent-review.json).

## Preserved pre221 checkpoint

Preserved pre-221 checkpoint: **93/93 owner tests pass, including all eleven composed cases**, in `standalone-ci-five-group-final-03/website-composed-tests/owner-release`, with confirmed child exit 0, no skipped cases, failures, unexpected I/O or external network attempts. Seven families plus four extended scenarios retain 30 accepted fixture-carrier calls. That composition JSON SHA256 is `196dce0a163762289ea768c570cf96d02d6866c77301c0b41cf7b0a398d05066`. The [exact five-group CI](2026-09-09-sms-standalone-ci-evidence.md) completed 121/121 and binds the archived source. This is local execution, not deployed certification.

Actual migrations 215 and 217 now run in the shared fixture, with genuine baseline payment columns/defaults. Generic cache fixtures retain their exact original bytes under the actual content-derived immutable key and signature; the renderer remains forbidden, and held/stale/released/exact-byte assertions all pass. The composition binds 44 named source inputs.

## Historical fixture and integration checkpoints

The prior BE01 checkpoint passed 93 cases in `be01-fixture-checkpoint-02/owner`; its composition SHA256 `383d846fe863668bf80da17c58f99958baf393f32428141a6e954594eca4f696` remains archived. The final five-group refresh above supersedes its pending-integration status.

The new saved-price reader exposed legacy test aliases and an unsigned generic PDF cache. Fixture amounts are unchanged; line items now use `quantity`/`unit_price_ex_gst`, the origin tier supplies `subtotal_ex_gst`, and generic PDFs carry the actual signature derived from the persisted quote. A narrow fixture projection models the fields selected by the real PDF context. Its undefined-to-null accommodation is not proof of production column availability. The real strict cache check and forbidden renderer remain active, as do held denial, released HTTP 200 and exact saved-byte assertions. Checkpoint 01 failed 24 cases before the projection/subtotal fixes; checkpoint 02 passes all 93. Independent review passes. See [fixture checkpoint evidence](2026-09-09-sms-be01-fixture-checkpoint.md).

Earlier integration refreshes checked both guards and the correction-aware origin resolver alongside owner approval, duplicate release and saved PDF cache paths. Attempt 05 correctly rejected an old fixture that inserted a commercial priced extraction before its owned parent; all eleven cases were skipped at setup. Attempt 06 passed after correcting that insertion order. The generic and painting origin cases also create a later-conversation correction through the real correction helper and task/outbox SQL before approval. Attempt 07 passed its eleven cases but failed overall because the final URL assertion incorrectly required a link in the two deliberately link-free correction acknowledgements. Attempt 08 passed after requiring those two exact acknowledgement bodies and retaining the canonical URL assertion for every other message.

A subsequent maintainability review found that the owner journey and painting-page test read an untracked, generated parity report. They now use `quotemate-automation/tests/fixtures/sms-owner-release-seed.json`, containing only `fixtureVersion`, provenance, and the identical synthetic F16 roofing quote/F17 painting estimate. It does not represent a passing parity run or live pricing authority. Both tests validate the expected fixture shapes and unchanged bytes; owner evidence records its hash/provenance and the painting-page suite logs the same metadata. Fixture SHA256 is `219e878e1a0e8c08c22c47e1942eb235fbf98e9cbf84f6309594a7076f3b8561`. Independent review and the full 93-case owner configuration passed this increment. Its two edited test files also passed scoped lint. Prior attempt 08 remains archived as `owner-release-before-reproducible-seed.json`.

## Confirmed defects and corrections

| Defect | Actual failure evidence | Required correction now implemented | Verification |
|---|---|---|---|
| Held painting and solar PDFs exposed cached prices | Actual anonymous public GETs returned HTTP 200 for unreleased saved rows. Existing roofing and aircon denial controls passed. | Check saved release authority before the cache helper; allow a verified owning bearer or cookie session to preview. Database read errors return 503, absent resources 404. | 14 actual-route tests pass with real cache/download helpers and fixture private PDF bytes. Renderer/network calls are prohibited. |
| Painting HTML defaulted to released after a failed second database read | The actual page rendered the held $18,648 price when the separate release/payment read failed. | Include release/payment fields in the first checked estimate read; derive access only from that row. The error state renders unavailable and held layouts contain no price. | Four actual-page static-rendering cases pass, plus eight publish-gate cases. |
| Painting held copy promised an imminent quote | The existing held-page text said the painter would send shortly despite approval still being required. | Saved-draft copy explicitly says painter review and approval are pending, without promising an imminent send. | Both held layouts reject the old imminent-delivery copy while a historical released quote remains visible. |
| Initial owner-approved SMS lacked its known originating conversation | Actual generic and painting approval POST fixtures contained an exact owned review-task relationship, but the release intent and dispatched message omitted `conversationId`. The dashboard manual-send route reproduced the same omission. | Resolve only exact owned task/resource, generic quote/intake or plan-upload relationships; revalidate customer, tenant and sending tradie number. Attach that ID to the durable intent before approval. Ambiguous/mismatched relationships or database errors fail before an outbox mutation. | 22 focused actual-route/helper cases and 29 existing route regressions pass. Actual SQL/outbox generic and painting approvals each publish one matching initial transcript; repeated owner POSTs preserve one intent and one send. |

The SMS-origin fix touches `lib/sms/quote-origin-conversation.ts` and the generic approve, generic manual-send and shared specialist quote-release POST routes. A same-phone conversation is never sufficient evidence. Portal-only quotes legitimately create an accepted outbox intent without an invented SMS conversation. A deliberate manual-send override to a different normalised phone remains sendable and unattached; original-customer source errors return HTTP 503. Existing immutable historical intents are not retroactively rewritten.

The public PDF fixes are confined to `app/api/q/paint/[token]/pdf/route.ts` and `app/api/q/solar/[token]/pdf/route.ts`; shared PDF generation/cache behavior remains intact. Both routes were already required website-contract entries. The painting page fix uses its first checked read and leaves price/release predicates unchanged.

GitNexus impact was run against the bound `quotemax` checkout before the production edits. The route/page entry points and new helper were UNKNOWN or unindexed, so the actual HTTP/page callers and route tests were confirmed by text search. No HIGH or CRITICAL result was ignored. The dashboard manual-send caller is `SendQuotePanel.tsx`; generic approval and shared specialist review have their existing approval UI callers.

## Seven-family composed acceptance

The runner is `quotemate-automation/tests/sms-owner-release-journeys.test.mjs`, using the dedicated `scripts/vitest-sms-owner-release.config.mjs` and `scripts/sms-owner-release-fixture.mjs`. It imports actual platform handlers and helpers, not compiled receptionist candidates. The schema adapter applies real work/outbox, owned-reference and approval SQL over a deliberately minimal local PGlite fixture schema, including migrations 198, 199, 201, 202, 205, 207, 211 and 212.

The original seven-case run passed for generic, roofing, painting, solar, plan, aircon and commercial-painting saved resources. Each case checked held customer content, wrong owner and stale review refusal, explicit owner approval, accepted durable intent, unchanged saved prices, duplicate approval, released public content and saved-reference resends in two conversations. Their seeded creation timestamps differ by one day. All five existing public PDF GET surfaces changed from held denial to HTTP 200 with the saved private PDF bytes; this harness does not invent public PDF GETs for aircon or commercial painting.

That baseline produced 21 accepted intents/fake-carrier calls: seven portal-origin releases and fourteen attributed same/new-conversation resends. Every resend was replayed with the same turn identity and retained one intent, one accepted carrier SID and one matching conversation transcript. The portal-origin release intentionally had no transcript association.

The passing extension adds stale owner revision and held final-quote handling, final-child authoritative resend selection, multiple-job disambiguation, wrong-tenant reference refusal, and generic/painting exact-origin accepted transcripts. The generic origin case also replays its approved initial intent through the actual manual-send endpoint. Both origin cases first create and replay a correction from a new conversation, preserving one correction task and the unchanged saved resource; approval still attaches to the original creation conversation. The full run records 30 accepted intents/fake-carrier calls: the original 21 plus final approval/resend, one selected-job resend, two initial approvals with proven SMS origins, and four correction messages (two owner notices and two customer acknowledgements). Every generated customer link must use the configured canonical website origin. The correction composition invokes the real helper, SQL and dispatch boundaries; actual inbound routing and job claims are covered separately.

The current JSON artifact is `2026-09-09-sms-owner-release-journeys.json` beside this report; its current hash is above. The historical four-group CI composition SHA256 is `e5d7fec3417ce7d114d4452ea60b30d7f554cebbf852ca4156770c229233905d`, archived in `be01-fixture-checkpoint-01/previous-reports/`. Its passing stdout remains `standalone-ci-final-01/inline-ci.log`, SHA256 `272fa83e0ca0193403c6a6ea695ed2a76972e4d325daac0add418d5a36a49a51`. Both checkpoints require fresh completion, empty failure/unexpected arrays, all eleven cases and a separate successful process exit. Named executed boundaries and fixture bytes are checked unchanged during each run; this is not a whole-import-closure attestation. The original parity input remains archived as `owner-release-parity-seed-attempt-08.json`, SHA256 `c005e7e48ad112a5fbfa8c58bf89f93031e93b5b30f8f6742225910c765274db`, as provenance only. Migration 211 SHA256 is `7b0b2f0892fd1fe67489a1d8a731ae518591347e0fc0b88763421e6e3676c909`; migration 212 is `4046d1bb6687c99025ad5e2e42692c66dc58906cc90e6f875ebe1ba68d5887d9`.

## Recorded commands and limits

Logs are under `C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/`.

- `owner-release-pdf-before-fix.log`: 2 failures and 2 passing denial controls; `owner-release-pdf-final.log`: 14 passed, exit 0.
- `owner-release-paint-page-before-fix.log`: one reproduced price leak and one overly specific old-copy expectation, with one passing control. `owner-release-paint-page-final.log`: 18 passed across the PDF/page files; `owner-release-paint-gate-unit.log`: 8 passed.
- `owner-release-origin-before-fix.log`: 2 failures proving omitted context in generic/painting approval. `owner-release-manual-origin-before-fix.log`: 3 failures and 1 passing alternate-recipient control.
- `owner-release-origin-final.log`: 22 passed, exit 0. `owner-release-origin-existing-routes-final.log`: 29 passed, exit 0. `owner-release-origin-lint-final.log`: scoped ESLint exit 0 with zero warnings/errors.
- `owner-release-journeys-attempt-03.log`: original 7-case SQL composition passed, exit 0. Earlier attempts failed on explicitly repaired fixture schema/UI/BOM omissions; no production assertion was weakened.
- `owner-release-journeys-attempt-04.log`: expanded 11-case SQL composition passed, exit 0. Command: `pnpm exec vitest run --config scripts/vitest-sms-owner-release.config.mjs tests/sms-owner-release-journeys.test.mjs --maxWorkers=1`. Each case retains a 20-second timeout.
- `owner-release-journeys-attempt-05.log`: new guard correctly rejects the orphaned fixture child; no test case passed at this setup-failure checkpoint. Its JSON is archived.
- `owner-release-journeys-attempt-06.log`: eleven cases passed with 211/212 active after correcting fixture insertion order. Its result remains archived in `owner-release-before-correction.json`.
- `owner-release-journeys-attempt-07.log`: eleven case bodies passed but the final acknowledgement assertion failed; overall exit 1 and `completed: false`. Its failed JSON is archived.
- `owner-release-journeys-attempt-08.log`: all eleven cases and final assertions pass, including actual correction-before-approval controls. The adjacent archived JSON binds this run. `correction-owner-harness-lint.log` records harness/fixture lint; `correction-owner-ack-lint-final.log` covers the final acknowledgement assertion edit.
- `standalone-ci-final-01/inline-ci.log`: the exact CI step passes all four groups, including 93 owner tests. Fresh owner JSON, Vitest results, offline result and empty network log are retained under `website-composed-tests/owner-release/`; `portable-seed-lint.log` records the two-test lint. See [standalone CI evidence](2026-09-09-sms-standalone-ci-evidence.md) for all four groups and exact command hashes.

Independent bounded source/test reviews passed for the public PDF gates, painting page/copy and exact-origin helper with all three owner-call routes. The eleven-case artifact is local website acceptance; compiled-service validation is recorded separately and must match the runtime source it exercises. The repository-fixture change in this refresh does not modify that runtime.

Auth identity verification and carrier acceptance are explicit local fixtures. Public pages execute as static server markup, not browser sessions; private PDF bytes are fixtures, not newly rendered visual PDFs. The saved quote, edited revision, final child, historical payment and next-day conversation are seeded records; their creation/edit/payment handlers and real elapsed time are outside this composed proof. Real inbound SMS routing, model/measurement providers, payment checkout, production RLS/PostgREST, deployment and carrier delivery are not certified by these tests. Unexpected database/storage/network paths are recorded and fail acceptance even if an optional caller catches an exception.
