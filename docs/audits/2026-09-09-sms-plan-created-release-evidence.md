# Plan upload through owner release: local composition

Status: **PASS for the offline same-row composition**, refreshed at `2026-09-09T07:32:45.639Z` by the exact standalone CI step with observed exit 0. The fresh result has `completed: true`, one passing case, empty failure/unexpected arrays, one model call, two actual pricer calls and five accepted fixture-carrier calls. This rerun retains the current correction-aware origin resolver and shared SQL fixture; the dedicated config also includes four separate [invitation delivery recovery cases](2026-09-09-sms-plan-invitation-recovery-evidence.md). All five cases pass; this upload-to-release test body is unchanged. Earlier passing checkpoints remain archived. No production result is claimed. The runner is `quotemate-automation/tests/sms-plan-created-release.test.mjs`, with `scripts/vitest-sms-plan-created-release.config.mjs` and the narrow `scripts/sms-plan-created-release-fixture.mjs` wrapper.

The earlier03 result remains archived separately, and04 passed this five-case group while six unrelated owner-PDF cache fixtures failed. Checkpoint05 reran all five standalone groups after that test-only repair; it passes121/121 with no declared or whole-app source drift. This plan/invitation test source was unchanged.

This closes a specific composition gap between the existing plan-worker recovery tests and the seeded seven-family owner-release tests. The new case submits multipart PDF bytes to the actual upload POST, executes migration 210's upload/work transaction, claims the actual fenced plan worker, runs the electrical pricing loader/pricer, and uses the resulting `plan_upload_requests.plan_extraction_id` through owner review, approval, public rendering and saved-reference resend. It does not manually insert a generated extraction or priced result.

The inputs are an existing owned upload invitation and SMS conversation, an explicit electrical pricing book, and one enabled tenant-owned double-power-point assembly. The extraction provider fixture returns two counted points. The expected real arithmetic is $100 materials plus $120 labour and $22 GST, totalling $242. The test requires the saved pricing-authority tenant/book identity, a notified review task, an accepted honest held-status message and no held public price. It then executes the actual owner quantity-confirmation PATCH and pricing POST on the same generated row before explicit approval, public access and accepted resends in the same and a new conversation. Duplicate upload/worker/approval/resend execution must preserve one upload, work item and extraction, one model call, two pricer calls (worker and owner confirmation), and five intended accepted messages. Every accepted intent must match exactly one carrier SID, tenant, sender, recipient and body. Known-origin customer messages must each have one matching accepted transcript; the same notified task remains after replay.

The existing owner fixture applies real migrations 198, 199, 201, 202, 205, 207, 211, 212, 215 and 217. The plan wrapper adds the required upload/request/extraction column shapes from the older plan schema and executes real migration 210, including the actual plan guard-readiness RPC used by owner editing/pricing. It supplies an in-memory private-storage service while retaining the actual `uploadPlanPdf` and `downloadPlanPdf` wrappers. The model seam checks that the downloaded bytes equal the submitted input. A transport denial is installed before application imports for fetch, HTTP/HTTPS, raw TCP/TLS sockets and UDP; unknown database/storage/network/renderer/post-response paths are recorded and fail acceptance even if callers catch their exceptions. Actual filestore provisioning is disabled by its `ESTIMATOR_CHATBOT_ENABLED=false` switch.

Run from `quotemate-automation/`:

```text
pnpm exec vitest run --config scripts/vitest-sms-plan-created-release.config.mjs --maxWorkers=1
```

Acceptance verified observed Vitest exit 0 plus the fresh [2026-09-09-sms-plan-created-release.json](2026-09-09-sms-plan-created-release.json) with `completed: true`, one passing result, empty failures/unexpected arrays, and 34 unchanged named source hashes. The two identical upload POSTs invoked the actual storage wrapper twice while retaining one stored input and one `plan_uploads` row; the worker downloaded that input once. One original notified task and extraction persisted through owner confirmation, approval and same/new-conversation resend/replay. A later edit to the stored hourly rate and completed-upload/worker replay did not regenerate the approved result. Each case retains a 20-second timeout. The runner is separate from the platform's ordinary TypeScript test inventory.

The result JSON SHA256 is `88bcddabe5ad34d8d308bb555181fd7c2e84afe23a9909bd5a96db3d8400fbcd`. Passing stdout is `standalone-ci-five-group-final-05/invocation.log`, SHA256 `3effe58faacfdd1834a4fa8ba8edb871b7fbfc82a2ae15d2b4cdb83bbed2631a`. These are under `C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/`. Plan attempts 01/02, `plan-created-release-before-correction.json`, `plan-created-release-before-invitation.json` and `plan-invitation-created-attempt-01-plan.json` retain the earlier passing checkpoints. The [exact CI refresh](2026-09-09-sms-standalone-ci-evidence.md) preserves the prior report in `standalone-ci-five-group-final-05/prior-compositions/` and fresh composition/runner/Vitest results under `website-composed-tests/plan-created-release/`, with an empty outer network log and confirmed child exit. Scoped lint is recorded in `owner-tool-plan-final-lint.log`, with the shared fixture edit covered by `correction-owner-harness-lint.log` and additive config/new invitation files by `plan-invitation-final-lint.log`. Independent source and evidence reviews passed for the prior increments; the CI refresh has its own evidence record.

Limits: extraction is a model-response fixture over header-valid PDF bytes, not a check of real PDF extraction accuracy. Verified owner identity, private storage service, PDF branding and carrier acceptance are fixtures. No report renderer, signed private-PDF distribution, filestore, browser overlay, initial SMS upload-invitation generation, live provider or actual carrier delivery runs. The local database has a minimal schema and one PGlite connection; production PostgREST/RLS compatibility, process loss and multiconnection contention are separate evidence. Source hashes bind named boundaries rather than the entire dependency closure.
