# SMS release prerequisites

This is a reviewable rollout sequence for the local SMS audit changes. It is not evidence that a migration or deployment has run. The [implementation record](2026-09-08-sms-receptionist-implementation.md) owns the current validation verdict and candidate identities.

**Combined release acceptance remains open.** The saved-roof follow-up defect is repaired and passes 275 focused cases, lint and [independent review](2026-09-09-sms-roofing-saved-followup-evidence.md). The original failed roofing parent and its six intermediate kill-case passes remain historical, alongside the 25 completed old-runtime normal/electrical/plumbing cases. A final current-source 55-case compiled matrix is still required; the superseded readiness receipt does not validate the repaired runtime.

Migration 221 and its paired callers implement atomic corrections, observed-revision checks and commercial rich-run pricing-proof validation. Readiness passed 211 checks and exact standalone CI passed 121/121 on their named inputs. Current platform validation combines the preserved full06 result (10,273 passed, one browser-fixture failure, 24 live skips), the independently reviewed test-only synchronization fix and 16/16 browser rerun, standalone typecheck07 exit 0, and build08 exit0 with all 84 pages. [Independent context verification](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/profile-correction-refresh-01/platform-context-independent-review.json) binds the unchanged 2,795-source/1,473-runtime maps. Full06 remains failed and build07 remains aborted; no all-green full-suite rerun is claimed. [Independent final fleet review](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/profile-correction-refresh-01/independent-final-review.json) passes five fresh trade builds, exact unchanged front-desk reuse, all six verifiers, five build HTTP smokes and 16 compiled controls on readiness `f95126d0af7467d03dbc9fae433f7bf81ab6b2c31f89a601567a0c3dfde1da0c`. Actual55 and five front-desk HTTP compositions remain separate gates. The prior 45 primitive cases carry forward by [verified executed-input equivalence](2026-09-09-sms-process-recovery-evidence.md); they were not rerun. Parity 2 will run fresh.

The [specialist PDF repair](2026-09-09-sms-specialized-pdf-immutability-evidence.md) passes all 115 focused cases and scoped lint. Its immutable-source review and exact CI 121 remain recorded on their stated inputs. Subsequent platform and fleet validation include that source; actual conversation/recovery and HTTP compositions remain separate.

The [independently verified prior fleet](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/final-platform-refresh-independent-review.json) binds the now-superseded readiness SHA256 `f3c2b52b3e9f29084ce74a197bbdd54c89f20350288a1caadbf85ca6172fdb2a` at `2026-09-09T08:19:53.0915812Z`, all six release identities and 2,764 complete compiled outputs. The five build HTTP smokes are distinct from the five pending front-desk/trade HTTP compositions.

## Source and database contract

Freeze the combined website, all six services, dependency locks, migration hashes and compiled-output attestations. Verify every candidate against that same website snapshot before promotion. A matching migration number alone does not prove matching SQL or applied database constraints.

**Preserved local validation before 221:** the earlier shared server and browser source handoffs are complete. The joined final/credit/balance and signed-receipt checkpoint passes 75 cases with actual 217; exact standalone CI passes 121 cases with 219; the paired follow-up operation contract in 220 passes 54 independent checks. These overlapping counts are separate checkpoints, not an aggregate total.

The current 218 compatibility gate passes **211/211 cases**: 131 actual SQL, 27 attestation, 6 schema, 22 website-closure, 2 release-identity and 23 health checks. Eight changed JavaScript/TypeScript files pass zero-warning lint. [Independent review](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/quote-chain-readiness-221-independent-review.json) verifies all 57 named archived/current files at that checkpoint and the exact results without drift; the two subsequent test-only deltas are recorded below. The immediately preceding 130-pass/1-fail SQL checkpoint reproduced insufficient receipt-table RLS authority; the narrow correction now checks that relation independently, with allowed-owner and forced-RLS controls. The [readiness evidence](2026-09-09-sms-quote-chain-readiness-evidence.md) retains the older 149-case, 48-case and restricted-function-owner checkpoints as history.

Two later test-only changes are explicitly bridged from the original 57-file readiness archive. The commercial correction helper's `owner: string` annotation has [identical emitted JavaScript](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/fleet-candidate-02/final-validation-2026-09-09/221-test-type-delta-independent-review.json) and its 29 cases pass in full06. The browser fixture now waits for its one asynchronous POST receipt capture before the unchanged exact assertion; [independent review](C:/Users/dalig/.codex/visualizations/2026/09/08/01a07f99-45aa-7301-bf72-b1e946ed3015/full06-browser-sync/independent-review.json) verifies the two added lines, three context-line ending changes and 16/16 focused pass. These two handoff tests were not among the six executed readiness files. The archive remains unchanged: 55 named files still match current bytes, with these two reviewed deltas. CI's334 inputs exclude both changes. Completed typecheck07/build08 validates the corrected tests and unchanged application source; old archive hashes are not rewritten.

Both quote creation routes, commercial pricing/save, run reads/legacy PATCH, explicit corrections, the current web caller and follow-up SMS, call, manual-event and history-read routes are mandatory website compatibility roots. Bounded platform and fleet validation above pass after the roofing repair; completed compiled-route/HTTP checks and controlled production acceptance remain required before this preparation inventory supports rollout.

The SMS team owns migration 218 for read-only verification of installed quote-chain, commercial pricing and follow-up contracts. Mobile owns the implemented 213–217, commercial pricing 219, follow-up migration 220 and commercial correction migration 221. The shared SMS release contract requires the exact nineteen migration files **198, 199, 200, 201, 202, 204, 205, 207, 210, 211, 212, 213, 214, 215, 217, 218, 219, 220 and 221**. Supported deletion 208 and provisioning 216 remain separate combined-website prerequisites. Readiness fingerprints/prerequisites follow the reviewed write functions; readiness cannot pass by calling a write RPC or by seeing a migration number. Source ownership and local validation do not establish that a migration has been applied.

Reconcile the target database's existing baseline first. The incremental release contract is not a fresh-database installer: it assumes the established conversation, trade, plan, quote-chain and push schemas, including migrations 122, 191, 193 and 194.

Apply only verified forward migrations that are missing from the target, in this compatible order:

| Migration under `quotemate-automation/sql/migrations/` | Dependency / purpose |
|---|---|
| `198_sms_durable_work.sql` | Durable receipts, jobs, checkpoints and ownership fences |
| `199_sms_delivery_outbox.sql` | Uses 198; durable outbound attempts and receipts |
| `200_frontdesk_durable_inbox.sql` | Durable front-desk routing and retry |
| `201_sms_trade_quote_contract.sql` | Uses 198/199; specialist persistence, owned references and review tasks |
| `202_job_quote_operations.sql` | Required by current website approval/send readiness; preserve the mobile-owned file |
| `204_sms_owned_quote_revisions.sql` | Uses specialist columns introduced by 201 |
| `205_generic_quote_customer_release.sql` | Uses 199; separate explicit release from delivery |
| `207_quote_pricing_versions.sql` | Required by the current estimator; preserve the mobile-owned file |
| `208_supported_quote_deletion.sql` | Required when releasing the combined website's supported deletion capability; depends on existing quote-chain schema, 198 and 202 |
| `210_sms_plan_work.sql` | Uses 198/201; producer cutover below is required before its immediate legacy-upload backfill |
| `211_commercial_quote_release_guard.sql` | Uses 201; keeps an approved commercial tender's priced source and public identity immutable |
| `212_plan_quote_release_guard.sql` | Uses 201; keeps an approved plan result immutable while allowing PDF-cache and worker-fence metadata updates |
| `213_prepare_balance_quote.sql` | Atomic owned paid-chain validation and one unpublished balance child; requires established quote-chain and historical-pricing schema |
| `214_prepare_final_quote.sql` | Atomic owned final-child preparation and lifetime final-child uniqueness; inspect existing duplicates before adding the unique index |
| `215_generic_release_snapshot.sql` | Upgrades 205 approval to exact equality of the complete reviewed snapshot, including 207 pricing version and report/parent identity; preserves old outbox evidence |
| `216_tenant_provisioning_attempts.sql` | Separate combined-website provisioning/recovery prerequisite; not included in the SMS quote-chain migration set solely because of its number |
| `217_final_quote_credit_settlement.sql` | Uses 199/205/207/215; records deposit-credit settlement from an accepted immutable reviewed intent and supports the owned final-quote reader; reviewed and exercised in the joined local chain |
| `218_sms_quote_chain_readiness.sql` | Catalog-only installed writer/dependency/schema verification; checks 29 actual function contracts and stays unready until actual 219, 220 and 221 dependencies are present and match; 211 local checks pass |
| `219_commercial_paint_pricing_proof.sql` | Preserves explicit versus inherited labour authority, tenant GST and exact source/proof/BOM through atomic pricing/save; reviewed handoff and actual standalone tool/release gates pass |
| `220_followup_operations.sql` | Uses the existing follow-up/contact schemas and 198/199; durable operation identity, accepted-evidence binding, recoverable ordered history and atomic manual notes; paired current callers required |
| `221_commercial_paint_correction_operations.sql` | Uses established 201/211/219 contracts; atomic correction and immutable operation receipt, observed-revision/latest-extraction validation, and fresh commercial rich-run approval bound to the exact reviewed pricing source/proof/generation |

The 220 source handoff is frozen at SHA256 `2f0a875a26626666e6daea7c0842de2370814b623cdc34e62aada5327fc1a8ee`. The [fourteen-file/eight-function source manifest](C:/Users/dalig/Desktop/MaintainTech/MaintainOrg/qm-mobile/specs/audits/evidence/c11-followup-source-freeze-2026-09-09.json) hashes full function definitions; the 218 catalog check separately pins normalized `pg_proc.prosrc` bodies. These are source identities, not evidence of installation.

Migrations 202, 207 and 208 are not generic replay-safe scripts. Do not rerun them solely because they appear in this list.

The owner-approval schema probe must check `job_quote_operations` fields `tenant_id`, `operation_id`, `request_hash`, `intake_id`, `quote_id`, `status`, `pinned`, `pin_requested`, `created_at` and `updated_at`. An empty valid table passes this schema check. A missing table or column fails readiness. An individual quote's draft-completion check remains a separate operation.

The read-only service-role RPCs `sms_commercial_quote_guard_ready()` and `sms_plan_quote_guard_ready()` must each return true. They inspect the actual enabled trigger functions and event coverage; a migration filename alone is insufficient. The affected edit/pricing APIs also perform a bounded probe before writing and return 503 if protection cannot be confirmed. The guards reject changes to published results, their ownership/token, and removal or clearing of their release. A tradie must create a new tender or plan result for changed pricing; the approved old link retains its original result.

The additional `sms_quote_chain_ready()` probe must return true against finalized migration 218, SHA256 `b081dd4214501f959dfa42208e98cb55752a08a261be343d02dff8ed0ce9ba82`. Its catalog-only contract checks 29 actual function bodies, typed/named/default arguments, return/security metadata and execute grants, plus required columns, operation nullability/defaults/checks, unique identities and enabled triggers. It also checks each SECURITY DEFINER owner's required table and row-lock privileges, effective RLS authority, schema access and directly called helper permissions, and the service role's authority over the invoker correction receipt table. Entitled non-superuser owners are valid; lost authority fails readiness. The probe never prepares a quote, approves it or settles credit. The actual SQL controls verify that installation before 219/220/221 remains unready until those dependencies are restored. Website and generated trade readiness must propagate a false/error result, and front desk must propagate an unready downstream service. Local PGlite validation does not establish the target database's installed contract or production concurrency.

Migration 215 changes the review-hash shape even when its added fields are null. Deploy compatible callers together; do not rewrite historical outbox payloads or use a new automatic operation key to escape a mismatch. An existing operation retains its saved recipient, body and delivery state. Readback is not permission to approve altered content. Pending or ambiguous acceptance must follow the original durable worker/reconciliation policy.

Migration 219 also requires compatible commercial Price/Save callers. New saves carry both `pricingProof` and the exact `pricedAt` returned for the reviewed BOM; preserve the pair through retries. The timestamp distinguishes identical repricing generations and retains PostgreSQL microseconds. Read-only recovery returning `not_found` does not prove an earlier uncertain POST cannot still commit, so it cannot erase the retained receipt. The down migration removes the new RPCs while retaining proof history; gate pricing/save traffic before a compatible rollback. The [shared commercial handoff](C:/Users/dalig/Desktop/MaintainTech/MaintainOrg/qm-mobile/specs/audits/commercial-paint-pricing-proof-2026-09-09.md) records the reviewed contract and source hashes.

Migration 221 is frozen at SHA256 `b41774584cc12a16ecca543bc2e2a58e3d03ad21a8effcb4e12d9f6f87341940`. The [commercial correction contract](../../quotemate-automation/docs/commercial-paint-correction-contract.md) defines the current web caller's actual GET baseline, retained operation identity, explicit revision and GET-only uncertain-outcome recovery. Legacy PATCH now validates and commits atomically using an ingress snapshot; it cannot prove an old client's previously observed revision or retained retry identity. It therefore does not establish current-client stale-edit or recovery acceptance. After a committed-operation readback, clients refresh the actual current state before subsequent pricing; an earlier receipt cannot overwrite a newer correction.

Migration 211 locks the commercial parent when protecting priced extraction writes. PostgreSQL can abort a parent/child lock-order conflict with a concurrent release; callers must recover from that failed operation. Local PGlite checks establish the guards and fail-closed behavior, not production multi-connection availability or a deadlock-free claim. Migration 212 and plan release lock the same extraction row.

## Cutover and verification

**Prompt release gate:** New scoped SMS prompts remain unshipped pending the required 100 anonymized holdout evaluation and published five-dimension baseline/candidate delta; scripted provider fixtures are not that evaluation. The scoped fallback in `lib/sms/scoped-fallback.ts` is wired into generated roofing, painting and solar dialogs. Its unit and compiled fixture checks establish their stated behavior, not model quality.

[AGENTS.md](../../AGENTS.md) requires delta measurement before a prompt change ships. The [strategy evaluation framework](../strategy.md#6-the-eval-framework--built-first) specifies 100 historical intake-to-final-quote pairs from the first three pilot tradies, anonymized and tagged by trade, job type and complexity. Score scope completeness, price accuracy, line-item correctness, exclusions and confidence calibration from 0–5; publish baseline/candidate results per trade and job type. Revert a prompt change if its average score drops more than 5%. No live model evaluation or deployment has been performed by this audit checkpoint. Commercial 221 now passes local platform, approval and readiness validation; the prompt evaluation gate remains open alongside the remaining compiled/HTTP compositions and controlled production acceptance.

The checked-in [holdout inventory](../../quotemate-automation/eval/holdout-pairs.json) contains only five ungraded starter placeholders: three electrical and two plumbing. All fifteen expected tier prices are null; the pairs have no complexity labels or verified historical provenance. The [quote evaluation script](../../quotemate-automation/scripts/eval-quotes.mjs) is a six-dimension deterministic-pricing scorecard skeleton, with its live runner and baseline comparison still marked TODO. It does not implement the required five-dimension scoped-prompt baseline/candidate evaluation, and a successful structural run cannot satisfy that gate.

The [original SMS sweep](../../quotemate-automation/scripts/sms-sweep-manifest.json) contains 43 synthetic service prompts (20 electrical, 23 plumbing); the [combined manifest](../../quotemate-automation/scripts/sms-sweep-combined-manifest.json) adds 25 adversarial scenarios. Its [grader](../../quotemate-automation/scripts/sms-sweep-evaluate.mjs) uses keyword recognition, question counts and routing patterns. Those historical sweep captures and the [scoped-fallback tests with mocked model responses](../../quotemate-automation/lib/sms/scoped-fallback.test.ts) provide their stated regression evidence. They are not anonymized, tradie-verified intake-to-final-quote holdouts or a measured delta for the new prompts.

The authored website/trade workflow is `.github/workflows/sms-fleet-contracts.yml`; the separate front-desk repository owns `.github/workflows/sms-contracts.yml`. Its image job requires `QUOTEMAX_PLATFORM_REPOSITORY` and an exact forty-character `QUOTEMAX_PLATFORM_REF`, plus a read token only if the pinned platform checkout requires it. Configure both repository gates in promotion policy; the root aggregate cannot enforce the other repository's result.

The image jobs build the candidate's actual Dockerfile, probe that immutable image ID with network disabled and inert fixtures, verify its manifest and compiled inventory, and archive the same tested image. Explicit Bash enables pipeline failure propagation for image build/save commands; diagnostic artifacts upload on failure. These workflow definitions and their local protocol/source tests are not evidence that Docker, remote CI or promotion has run. Actual image execution remains required before rollout.

Read-only checks found no usable `docker`, `podman` or `nerdctl` CLI through Windows command discovery, known Docker Desktop executable locations or command discovery inside the already-running Ubuntu WSL2 environment. This bounded check did not inspect every custom binary or engine socket; it did not install or start a runtime. Execution of the six actual service images and their remote CI jobs remains unverified; compiled local service checks do not close those gates.

The website/trade workflow also requires five standalone test groups for actual tool creation, residential form/SMS comparison, created-tool release, uploaded-plan/invitation recovery and owner approval/public rendering. These `.mjs` suites are outside the default TypeScript suite. Each group must exit successfully with fresh completed composition reports; a stale report cannot satisfy the gate. The form report must contain electrical, plumbing and roofing exactly once each. The owner cases use committed synthetic inputs, while commercial confirmation/pricing execute actual 221/219 over explicit tenant rates. The exact five-group inline command passes121 local cases in checkpoint05, including both commercial approval paths and immutable cached PDF access;334 archived input hashes match before/after/current files. The failed115/6 cache-fixture checkpoint04 and older219/four-group checkpoints are retained. The [standalone CI evidence](2026-09-09-sms-standalone-ci-evidence.md) records the exact workflow and result identities; remote execution remains required.

1. Require the website/trade CI result, the separate front-desk result and the candidate verifiers before any traffic admission. The root aggregate does not enforce the external front-desk repository's workflow. Docker images, remote CI and production promotion rules require their own evidence.
2. Stop admission to legacy plan-upload producers and resolve their tracked in-flight `after()` handlers. Keep platform plan consumers paused. Migration 210 enqueues legacy retained uploads immediately; overlapping old producers and new workers could otherwise process one upload twice.
3. Apply the verified schema changes. Preserve acknowledged routing/work receipts, saved resource identities and outbound attempts throughout the transition.
4. Deploy the matching website with its approval, public-page, delivery-status and durable plan boundaries. Keep general production routing gated; admit only designated controlled test traffic to the verified trade workers and front desk initially.
5. Run the audit's controlled tenant/recipient cases through the selected deployment: each enabled tool and routed trade, explicit owner approval, saved-link recovery, public rendering, carrier failures and out-of-order delivery receipts. Record real deployment IDs, schema identity and message/resource correlations. Offline fixture evidence does not satisfy this step.
6. Enable general production routing and resume retained plan jobs on the durable website only after the relevant controlled acceptance and compatibility checks pass. Preserve a recoverable state for any traffic held during cutover.

The legacy `railway-deploy-receptionists.mjs` command is disabled and exits unsuccessfully before reading credentials, spawning subprocesses or contacting providers, including with `--vars-only`. Its output points to the validated candidate procedure. Offline tests exercised that refusal; no deployment or provider mutation was performed.

## Recovery and rollback

Drain work across every service before a compatible rollback and preserve receipts/outbox records. Migration 198's rollback refuses outstanding work; migration 210's rollback requires plan workers disabled and drained. Do not blindly execute all down files: the 199 and 200 rollback scripts drop delivery and routing ledgers.

Removing 211 or 212 removes a published-result safety boundary. Stop the corresponding edit/pricing traffic before a compatible rollback; never restore an old writer while customer links depend on these guards. Missing guard readiness must continue to block new writes and fleet quote capability.

The 214 down script removes its preparation function and lifetime final-child unique index; it does not remove or merge existing quote children. Its forward migration must first be checked for duplicate existing final children, and conflicting records require an explicit remediation decision. The 215 down script restores the older approval function while retaining approval stamps, pricing versions and outbox evidence. That rollback removes the stronger snapshot guard and therefore needs a matching backend rollback under gated traffic. The 217 down script removes the credit trigger/functions but intentionally retains its settlement ledger and payment evidence; readers and senders using that contract must be coordinated before removal. Removing 218 must leave quote capability unready until a compatible probe is restored. These are source-level rollback observations, not proof of a rehearsed production rollback.

No production rollback, migration, provider mutation, payment or customer SMS was performed during this review.

Migration 220 deliberately has no destructive down file. Its [reviewed rollback procedure](C:/Users/dalig/Downloads/QuoteMate/quoteMate/quotemate-automation/docs/followup-operations-rollback.md) pauses new authenticated POST actions with `FOLLOWUP_MUTATIONS_DISABLED=true` while retaining GET reconciliation, operation identities, outbox/provider evidence, history and acceptance triggers. Existing in-flight operations may still finish. Carry the paused compatible handlers into any application rollback; do not restore pre-220 unsafe producers or infer that a timeout prevented acceptance. Fix forward and verify the paired contract before re-enabling.

Migration 221's [operational rollback](../../quotemate-automation/sql/rollbacks/221_commercial_paint_correction_operations.sql) replaces only the apply RPC to pause new corrections. It preserves exact committed-operation replay, GET recovery, the receipt table, current rich-run approval checks and published-source immutability. It neither drops recovery history nor restores the older unbound commercial approval. Because this pause changes an exactly pinned function body, the expanded 218 probe intentionally reports incompatibility until the reviewed forward body or a separately verified compatible contract is restored. Do not assume other quote traffic remains ready while that shared probe is false; inspect the actual website and fleet readiness before admitting traffic.
